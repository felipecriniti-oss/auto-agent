---
phase: 09-matching-engine
plan: 02
plan_id: 09-02
slug: preview-pane-caller-fix
title: WishlistPreviewPane — drop enforcePfOnly arg + verify mock listings already PF-stamped
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/forms/WishlistPreviewPane.tsx
autonomous: true
requirements_addressed: []
requirements_notes: "Implements caller side of D-07 from 09-CONTEXT.md."
tags: [matching, caller-fix, preview-pane]
must_haves:
  truths:
    - "WishlistPreviewPane calls matchListingToWishlists with no third argument (default options)"
    - "Preview pane still produces non-zero counts for valid wishlists against the PF-stamped mock dataset"
    - "PREVIEW_LISTINGS continues to have seller_type 'PF' on every entry (no regression)"
  artifacts:
    - path: "src/components/forms/WishlistPreviewPane.tsx"
      provides: "Caller of matchListingToWishlists aligned with the new flag-free API"
      contains: "matchListingToWishlists(listing, [pending])"
  key_links:
    - from: "src/components/forms/WishlistPreviewPane.tsx"
      to: "src/lib/matching/engine.ts"
      via: "default-options call (no MatchingOptions overrides)"
      pattern: "matchListingToWishlists\\(.*\\)"
---

<objective>
Plan 09-01 removes `MatchingOptions.enforcePfOnly`. The only known consumer that passed `false` is `WishlistPreviewPane.tsx:67`. Drop the third argument so the call uses the now-default behavior (PF-only as a hard rule). The mocks at `src/lib/mock-data/preview-listings.ts` already carry `seller_type: "PF"` on every one of the 20 entries (verified during planning), so the count produced by the preview pane will not change for any well-formed wishlist input.

Purpose: complete the API change started by Plan 09-01 — without this, `pnpm typecheck` fails on the now-removed `enforcePfOnly` property.
Output: a one-line edit + a regression-style verification that the mock dataset is still 20 PF rows.
</objective>

<execution_context>
@C:/Users/pc/auto-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/auto-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-matching-engine/09-CONTEXT.md
@.planning/phases/09-matching-engine/09-01-PLAN.md
@.planning/phases/09-matching-engine/09-01-SUMMARY.md
@src/components/forms/WishlistPreviewPane.tsx
@src/lib/mock-data/preview-listings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the enforcePfOnly argument from the preview pane call</name>
  <files>src/components/forms/WishlistPreviewPane.tsx</files>
  <read_first>
    - src/components/forms/WishlistPreviewPane.tsx lines 60-78 (the matchListingToWishlists call site)
    - 09-01-SUMMARY.md (confirm Plan 09-01 actually shipped the engine change)
  </read_first>
  <action>
At line 67 of `src/components/forms/WishlistPreviewPane.tsx`, change:

`const results = matchListingToWishlists(listing, [pending], { enforcePfOnly: false });`

to:

`const results = matchListingToWishlists(listing, [pending]);`

Do not add a comment explaining the change — this is a clean caller-side removal. The behavior change is documented in 09-01.

After the edit, run `pnpm typecheck` to confirm no other typing leak remains.
  </action>
  <verify>
    <automated>cd C:/Users/pc/auto-agent &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "enforcePfOnly" src/` returns 0 lines across the entire `src/` tree
    - `pnpm typecheck` 0 errors
    - The matchListingToWishlists call at WishlistPreviewPane.tsx (around line 67) has exactly two arguments
  </acceptance_criteria>
  <done>Preview pane now uses the default (PF-only) engine contract.</done>
</task>

<task type="auto">
  <name>Task 2: Sanity-check the mock dataset is still uniformly PF</name>
  <files>src/lib/mock-data/preview-listings.ts</files>
  <read_first>
    - src/lib/mock-data/preview-listings.ts (verify count of seller_type entries)
  </read_first>
  <action>
Run two grep checks against `src/lib/mock-data/preview-listings.ts`:

1. Count of `seller_type:` lines must equal 20
2. Count of `seller_type: "PF"` lines must also equal 20

If both checks pass, the mocks are correctly PF-stamped — no edit required. Do NOT touch the file.

If either check fails (any non-PF entry leaked in since planning), edit the offending line(s) to set `seller_type: "PF"` and re-run the checks. Do not change any other field.

After the checks (and any remediation), confirm the engine still passes cleanly:
`pnpm test src/lib/matching/ --run`
  </action>
  <verify>
    <automated>cd C:/Users/pc/auto-agent &amp;&amp; pnpm test src/lib/matching/ --run</automated>
  </verify>
  <acceptance_criteria>
    - Counts: total `seller_type:` lines = 20 AND `seller_type: "PF"` lines = 20
    - `pnpm test src/lib/matching/ --run` passes (24 tests after Plan 09-01)
  </acceptance_criteria>
  <done>Mock dataset confirmed uniformly PF; engine tests still green.</done>
</task>

</tasks>

<verification>
- `grep -rn "enforcePfOnly" src/` returns 0 lines (entire codebase)
- `pnpm typecheck` 0 errors
- `pnpm test src/lib/matching/ --run` passes with 24 tests
- `pnpm biome check src/components/forms/WishlistPreviewPane.tsx` 0 issues
</verification>

<success_criteria>
- Preview pane caller aligned with the new engine API
- Mock dataset verified PF-uniform (20/20)
- D-07 fully delivered across both engine and caller
</success_criteria>

<output>
After completion, create `.planning/phases/09-matching-engine/09-02-SUMMARY.md` describing:
- The single-line edit at WishlistPreviewPane.tsx:67
- Mock-data sanity-check counts (expected 20/20)
- Confirmation that no enforcePfOnly references remain anywhere in src/
</output>
