---
phase: 01-chat-manual-funcional
plan: 08
type: execute
wave: 5
depends_on: [01-07]
files_modified:
  - .github/workflows/ci.yml
  - vercel.json
  - README.md
autonomous: false
requirements: [INFRA-02]
tags: [deploy, vercel, ci, smoke-test, human-verify]

user_setup:
  - service: vercel
    why: "Public deploy + preview branches per INFRA-02"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API Keys"
      - name: ANTHROPIC_MODEL
        source: "Set literally to claude-sonnet-4-5-20250929"
      - name: NEGOTIATION_ENABLED
        source: "Set literally to true (flip to false to kill endpoint)"
    dashboard_config:
      - task: "Create Vercel project"
        location: "Vercel Dashboard → Add New... → Project → Import the git repo"
      - task: "Configure env vars (Production + Preview)"
        location: "Vercel Project → Settings → Environment Variables"
      - task: "Enable preview deployments"
        location: "Vercel Project → Settings → Git (defaults are fine — every PR creates preview)"
  - service: anthropic
    why: "Phase 1 agent runs on Claude Sonnet 4.5"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys → Create Key"

must_haves:
  truths:
    - "Public Vercel production URL loads the playground in <2s (DevTools Network tab check)"
    - "Preview deployments fire automatically on PRs (INFRA-02)"
    - "Felipe can complete a real live negotiation end-to-end: form → FIPE → 4-6 rounds streaming → encerrar → summary"
    - "Setting NEGOTIATION_ENABLED=false in Vercel env vars and redeploying (or waiting for next invocation) causes the page to show the red kill-switch banner and 503 the endpoint"
    - "Setting NEGOTIATION_ENABLED=true restores normal operation"
    - "GitHub Actions CI runs on every PR: pnpm install → biome check → typecheck → vitest run → next build; all steps green"
    - "README.md contains the deploy instructions + env var list + kill-switch instructions"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline covering lint + typecheck + test + build"
      contains: "pnpm"
    - path: "README.md"
      provides: "Deploy walkthrough + env vars + kill-switch documentation"
      contains: "Vercel"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm biome check + pnpm vitest run + pnpm build"
      via: "runs-on: ubuntu-latest, node-version: 22"
      pattern: "pnpm"
---

<objective>
Ship the playground: add a minimal CI workflow, document Vercel setup, deploy to production, and run the human smoke test that confirms all 5 ROADMAP Phase 1 Success Criteria are true on the live URL.

Purpose: INFRA-02 (Vercel deploy + preview) + final phase sign-off. This is the moment the playground becomes real: a public URL Felipe can open in front of a lojista.
Output: One CI workflow file, an updated README with deploy docs, a live production Vercel URL, and a signed-off human verification of every Phase 1 success criterion.
</objective>

<execution_context>
@C:/Users/pc/auto-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/auto-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-chat-manual-funcional/01-CONTEXT.md
@.planning/phases/01-chat-manual-funcional/01-RESEARCH.md
@.planning/phases/01-chat-manual-funcional/01-VALIDATION.md
@.planning/phases/01-chat-manual-funcional/01-07-SUMMARY.md
@.planning/ROADMAP.md
@PROJECT-BRIEF.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel env vars → runtime | Secrets live in Vercel dashboard, never in repo; `.env.local` gitignored |
| GitHub Actions → pnpm install | Lockfile pinned via `--frozen-lockfile`; no arbitrary network fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | ANTHROPIC_API_KEY leak via logs / console | mitigate | Vercel encrypts env vars at rest; Plans 04/06 already ensure no logging of the key in error paths |
| T-08-02 | Tampering | Supply-chain attack via pnpm-lock changes | mitigate | CI runs `pnpm install --frozen-lockfile`; commits to lockfile go through PR review |
| T-08-03 | Availability | Kill switch env-var flip breaks demo | accept | That's the point — it's a manual circuit breaker. Documented. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub Actions CI + vercel.json + deploy docs in README</name>
  <files>.github/workflows/ci.yml, vercel.json, README.md</files>

  <read_first>
    - README.md (Plan 01 version)
    - PROJECT-BRIEF.md §6 (Node 22 LTS)
    - .planning/phases/01-chat-manual-funcional/01-RESEARCH.md §Environment Availability (env var checklist)
    - .planning/phases/01-chat-manual-funcional/01-RESEARCH.md §Known Pitfalls (pitfall #5 — edge runtime bundle size)
  </read_first>

  <action>
1. Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install (frozen)
        run: pnpm install --frozen-lockfile

      - name: Biome check
        run: pnpm biome check src

      - name: Typecheck
        run: pnpm typecheck

      - name: Vitest
        run: pnpm vitest run

      - name: Next build
        run: pnpm build
        env:
          # Build does not need live secrets — dummy values avoid gating.
          ANTHROPIC_API_KEY: sk-ant-ci-dummy
          ANTHROPIC_MODEL: claude-sonnet-4-5
          NEGOTIATION_ENABLED: "true"
```

2. Create `vercel.json` (minimal — Next 15 on Vercel defaults are correct, but we want explicit edge runtime documentation + ensure that `/api/negotiate` and `/api/fipe` have sensible maxDuration):

```json
{
  "functions": {
    "src/app/api/negotiate/route.ts": { "maxDuration": 60 },
    "src/app/api/fipe/route.ts": { "maxDuration": 30 }
  }
}
```

3. Append to `README.md` — ADD a new section below the existing "Scripts" block. Do NOT remove existing content; extend it:

```markdown
## Deploy — Vercel (INFRA-02)

1. **Importar projeto:** https://vercel.com/new → importe este repositório. Aceitar os defaults do Next 15.
2. **Configurar env vars** (Settings → Environment Variables, para `Production` e `Preview`):
   - `ANTHROPIC_API_KEY` — copiar de https://console.anthropic.com/settings/keys
   - `ANTHROPIC_MODEL` — `claude-sonnet-4-5-20250929`
   - `NEGOTIATION_ENABLED` — `true` (mudar para `false` para desligar o endpoint sem redeploy)
3. **Deploy:** push na `main` → Production; abrir PR → Preview (automático via git integration).

### Kill switch (INFRA-03 / D-17)

Para desligar o endpoint `/api/negotiate` sem deploy:
- Vercel Project → Settings → Environment Variables → `NEGOTIATION_ENABLED` = `false`
- O próximo request retornará 503; o frontend mostrará banner vermelho "Negociações temporariamente indisponíveis".
- Para religar: volte `NEGOTIATION_ENABLED` para `true`. Efeito é per-request — não precisa redeploy (Vercel propaga env changes quase instantaneamente).

### Rate limit (INFRA-03 / D-16)

- `/api/negotiate`: 5 requests por IP por 60s
- `/api/fipe`: 20 requests por IP por 60s
- Bloqueio: 429 com header `Retry-After`. Frontend mostra toast sonner.

### Custos (Anthropic Sonnet 4.5)

Cada sessão de 6 rodadas custa ~$0.05–0.08 USD. Phase 1 demos < 20 sessões/dia = < $2 USD/dia.

## Performance

- Target < 2s primeiro render (ROADMAP success criterion #5).
- Edge runtime em `/api/negotiate` e `/api/fipe` (menor latência streaming no Brasil).
- Bundle edge < 1MB (free tier) — confirmar no primeiro deploy.

## CI

GitHub Actions em `.github/workflows/ci.yml`. Roda em cada PR:
- `pnpm install --frozen-lockfile`
- `pnpm biome check src`
- `pnpm typecheck`
- `pnpm vitest run`
- `pnpm build`
```

4. Verify the CI workflow passes locally by running the exact same commands:

```bash
pnpm install --frozen-lockfile && pnpm biome check src && pnpm typecheck && pnpm vitest run && pnpm build
```

All five must exit 0 before proceeding.
  </action>

  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm biome check src && pnpm typecheck && pnpm vitest run && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and contains all 5 steps (biome, typecheck, vitest, build) plus install
    - `.github/workflows/ci.yml` uses `node-version: 22` (brief §6)
    - `.github/workflows/ci.yml` uses `pnpm install --frozen-lockfile`
    - `vercel.json` exists with `functions` entries for both API routes
    - `README.md` contains a new heading `## Deploy — Vercel (INFRA-02)`
    - `README.md` lists all 3 env vars: `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL`, `NEGOTIATION_ENABLED`
    - `README.md` documents the kill-switch flip procedure with literal value `false`
    - `README.md` documents rate-limit numbers: 5/60s for negotiate, 20/60s for fipe
    - All 5 CI-equivalent commands exit 0 locally
  </acceptance_criteria>

  <done>
  CI workflow and deploy docs landed. Local run of the exact CI commands is green. Repo is ready for first Vercel import.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human deploys to Vercel and sets env vars</name>
  <what-built>
    The repo contains `vercel.json`, `.github/workflows/ci.yml`, updated `README.md`. The production code (all Plans 01-07) is committed and the CI is green locally.
  </what-built>

  <how-to-verify>
1. Open https://vercel.com/new in a browser.
2. Import the git repo for this project (`autoagent-playground` or whatever the repo is named).
3. On the import page, accept the default Next.js framework detection — do NOT override the build command.
4. Before clicking Deploy, scroll to "Environment Variables" and add exactly these 3:
   - `ANTHROPIC_API_KEY` = `sk-ant-...` (from https://console.anthropic.com/settings/keys → create a new key if needed)
   - `ANTHROPIC_MODEL` = `claude-sonnet-4-5-20250929`
   - `NEGOTIATION_ENABLED` = `true`
   Apply each to both `Production` and `Preview` environments.
5. Click Deploy. Wait for the first build (~2-3 minutes).
6. Once deployed, open the production URL shown.
7. Paste the production URL and the preview URL into the resume signal below.
  </how-to-verify>

  <resume-signal>
    Paste one of:

    `Deployed. Production URL: <url>. Preview URL (any PR): <url or "not yet tested">.`

    OR

    `Blocked: <reason>` (e.g., Anthropic API key quota exhausted, Vercel build failed with error X, git repo not yet pushed).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke test — run a real negotiation on the live URL and verify all 5 ROADMAP success criteria</name>

  <what-built>
    Live Vercel production URL (from Task 2) runs the full playground: 3-column layout, FIPE auto-fetch, streaming chat with Anthropic, summary panel with argument list, localStorage persistence, kill switch, rate limit.
  </what-built>

  <how-to-verify>
Execute these 5 steps in order, on the live Vercel production URL, and report pass/fail for each.

**Criterion 1 — Form + FIPE + targetPrice (ROADMAP #1):**
1. Open a real car listing on OLX or WebMotors. Copy: marca, modelo, ano, km, preço pedido, cidade.
2. Open the live URL.
3. Fill in the form with those values + estimates for diasOnline and reducoes.
4. Confirm FIPE auto-fetch resolves (within ~3s of filling ano) — the "FIPE encontrada: R$ ..." message appears.
   - If Parallelum fails, the manual FIPE input shows "Serviço FIPE indisponível". Type a FIPE value manually — it should be accepted.
5. The NegotiationStatusBar should show Target price = roundly 75% of FIPE, Walk-away = 90% of FIPE.
PASS / FAIL: ___

**Criterion 2 — Streaming char-by-char + bubble per turn + autoscroll + typing indicator (ROADMAP #2):**
6. Click "Iniciar negociação".
7. Observe the agent's opener message. Confirm:
   - 3-dot typing indicator appears briefly before the first chunk (D-05 phase a)
   - Text streams in visibly (token-by-token; looks like char-by-char) with ▌ cursor at end (D-05 phase b)
   - When stream completes, cursor disappears and bubble becomes static (D-05 phase c)
   - Bubble is left-aligned (agent), slate-100 background, tail on bottom-left (D-02)
8. Type a reply ("Não consigo vender por menos de R$ X") in the seller box, send.
9. Observe agent response — new bubble, streaming, then finalized.
10. Scroll up during the 4th streaming response. Confirm:
    - Chat does NOT force scroll (D-06 pin-on-scroll-up)
    - "↓ Nova mensagem" floating button appears
    - Clicking it scrolls to bottom
PASS / FAIL: ___

**Criterion 3 — Encerrar + summary with KPI cards + argument list (ROADMAP #3):**
11. Click "Encerrar negociação" in the ChatView header.
12. AlertDialog appears asking for confirmation. Click "Encerrar".
13. Check Browser DevTools Network tab — confirm the `/api/negotiate` request shows "cancelled" (D-11 AbortController verified).
14. SummaryPanel replaces ChatView.
15. Confirm 4 KPI cards visible:
    - Rodadas: N/6
    - Preço inicial / final (extracted from agent messages)
    - % redução vs Preço pedido (emerald ≥20%, amber 10–20%, red <10%)
    - % redução vs FIPE (same color coding)
16. Confirm "Argumentos usados" section shows at least 1 argument with a Lightbulb icon (D-14 extraction works).
17. Click "Ver conversa completa" — ChatView mounts in readonly mode; Back button returns to summary.
18. Click "Nova negociação" — form re-enables, page returns to idle state, prior session moved into localStorage `history`.
PASS / FAIL: ___

**Criterion 4 — localStorage persistence (ROADMAP #4):**
19. After one completed session, reload the page (F5).
20. Open DevTools → Application → Local Storage → autoagent-playground-v1. Confirm the JSON contains `history` with ≥1 ended session.
21. Start a new negotiation. Reload mid-stream. The form values should persist (or show the partially-completed session depending on timing) — at minimum, the store should not reset to blank on reload.
PASS / FAIL: ___

**Criterion 5 — <2s load on public URL (ROADMAP #5):**
22. Open a fresh incognito window. DevTools → Network tab → "Disable cache" checked.
23. Navigate to the production URL.
24. Record the `DOMContentLoaded` time and the full `Load` time from the bottom of the Network tab.
25. Both should be under 2000ms for the initial render.
PASS / FAIL: ___ (record actual ms)

**Final sanity — kill switch smoke:**
26. In Vercel dashboard → Settings → Environment Variables, change `NEGOTIATION_ENABLED` from `true` to `false`. Save.
27. Reload the production URL within 10 seconds. Fill the form and click Iniciar.
28. Confirm the red "Negociações temporariamente indisponíveis" banner appears and the request to `/api/negotiate` returns 503.
29. Flip `NEGOTIATION_ENABLED` back to `true`. Reload. Confirm normal operation restored.
PASS / FAIL: ___

**Rate limit smoke:**
30. Click "Nova negociação" + "Iniciar" six times in rapid succession.
31. The 6th request should return 429 and a sonner toast: "Muitas negociações recentes. Tente novamente em breve."
PASS / FAIL: ___

Report a line of the form:
```
All 5 ROADMAP criteria passed. Kill switch verified. Rate limit verified. Production URL: <url>.
```

OR, if anything failed, report the failing criterion number + observed behavior + expected behavior.
  </how-to-verify>

  <resume-signal>
    Paste one of:

    `Phase 1 smoke test passed. All 5 ROADMAP criteria green. Production URL: <url>. [optional notes]`

    OR

    `Phase 1 smoke test FAILED: <criterion number> — <observed> vs <expected>. Rolling back to [plan].`
  </resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks:

1. `.github/workflows/ci.yml` green on first PR after merge
2. Production URL loads < 2s
3. Live negotiation end-to-end works (form → FIPE → stream → summary)
4. localStorage persists across reload
5. Kill switch flip works without redeploy
6. Rate limit triggers on 6th rapid request
7. All 5 ROADMAP Phase 1 Success Criteria marked PASS by human reviewer

If any step fails, either:
- Patch in a new plan (e.g., `01-09-PLAN.md` targeting the specific gap)
- Or — if a tuning issue like FIPE fuzzy-matching hit rate — document as known limitation and ship anyway (Phase 1 success criterion #5 is the only hard gate; tuning can slip to Phase 2 per RESEARCH.md Open Question #1).
</verification>

<success_criteria>
- [x] INFRA-02: Production URL public; preview deployments on every PR
- [x] CI workflow green (biome + typecheck + vitest + build)
- [x] All 5 ROADMAP Phase 1 Success Criteria verified on live URL by human smoke test
- [x] README documents deploy, env vars, kill switch, rate limit
- [x] Phase 1 ships
</success_criteria>

<output>
After completion, create `.planning/phases/01-chat-manual-funcional/01-08-SUMMARY.md` per template. Include:
- Production URL + preview URL sample
- Actual first-load time in ms (from DevTools)
- Smoke test results for all 5 ROADMAP criteria
- Kill switch verified Y/N
- Rate limit verified Y/N
- Any tuning notes or known limitations (e.g., FIPE fuzzy match hit rate if observed)
- Next step: `/gsd-transition 1` to move on to Phase 2 planning.
</output>
