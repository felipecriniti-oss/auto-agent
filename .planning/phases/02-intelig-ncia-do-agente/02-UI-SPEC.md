---
phase: 02
slug: inteligencia-do-agente
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-04-20
---

# Phase 2 — UI Design Contract: Inteligência do Agente

> Visual and interaction contract for Phase 2 frontend surfaces. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> All locked decisions are carried forward from Phase 1 codebase — no new design system choices introduced.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | New York |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react (named imports only, no barrel) |
| Font | var(--font-sans) — system font stack via Next.js |

Source: `components.json`, `src/app/globals.css`, Phase 1 codebase.

---

## Spacing Scale

Locked from Phase 1. 8-point Tailwind scale only. No exceptions for Phase 2.

| Token | Tailwind class | Value | Usage |
|-------|---------------|-------|-------|
| xs | p-1 / gap-1 | 4px | Icon gaps, star-to-star spacing in rating input |
| sm | p-2 / gap-2 | 8px | Compact badge/label spacing, tab inner padding |
| md | p-4 / gap-4 | 16px | Default card padding, form field spacing |
| lg | p-6 / gap-6 | 24px | Section padding, page-level gutters |
| xl | p-8 / gap-8 | 32px | Major layout gaps |
| 2xl | — | 48px | Not used in Phase 2 |
| 3xl | — | 64px | Not used in Phase 2 |

Exceptions: touch target minimum 44px height for star rating buttons (use `min-h-[44px]` on the star row wrapper).

---

## Typography

Locked from Phase 1. All text uses `font-sans`.

| Role | Tailwind class | Size | Weight | Line Height | Usage |
|------|---------------|------|--------|-------------|-------|
| Body | `text-sm` | 14px | 400 (normal) | 1.5 | Form labels, tab content, disclosure text, scorecard row text |
| Label / caption | `text-xs` | 12px | 500 (medium) | 1.4 | Badge text, uppercase section headers, status bar, star count label |
| Heading / card title | `text-sm font-medium` | 14px | 500 | 1.4 | Card titles, tab labels, section headers within panels |
| KPI display | `text-2xl font-bold` | 24px | 700 | 1.2 | Benchmark scorecard aggregate values (e.g. "23.4%") |

No new type sizes introduced. Phase 1 locked: `text-xs`, `text-sm`, `text-2xl`, `font-medium`, `font-bold`.

---

## Color

Locked from Phase 1 `globals.css` `@theme inline` block.

| Role | CSS var / Tailwind utility | Hex equivalent | Usage |
|------|---------------------------|----------------|-------|
| Dominant (60%) | `bg-background` / `bg-white` | #ffffff | Page background, card surfaces, tab panel backgrounds |
| Secondary (30%) | `bg-slate-50` / `bg-muted` | ~#f8fafc | Tab bar background, disclosure summary background, config panel inner surface |
| Accent — primary (blue) | `text-primary` / `border-primary` = `--color-primary` | #2563EB | Active tab indicator underline, "Ver pensamento" toggle when ON, focus rings |
| Accent — success (emerald) | `text-emerald-700` / `border-emerald-300` / `bg-emerald-50` | #059669 | Positive benchmark deltas (reduction >= 20%), quality rating >= 4 stars filled |
| Accent — warning (amber) | `text-amber-700` / `border-amber-300` / `bg-amber-50` | #D97706 | Benchmark reduction between 10–19%, quality rating 3 stars, config panel warning notice |
| Accent — danger (red) | `text-red-700` / `border-red-300` / `bg-red-50` | #DC2626 | Benchmark reduction < 10%, config panel destructive notice (session-change warning) |
| Accent — violet | `text-violet-700` / `border-violet-200` / `bg-violet-50` | #7C3AED | AgentThinking disclosure header, "Ver pensamento" toggle badge, rationale text highlight |
| Destructive | `bg-destructive text-destructive-foreground` | ~#DC2626 | No destructive actions in Phase 2 surfaces |
| Border | `border-border` / `border-slate-200` | ~#e2e8f0 | Card borders, tab borders, disclosure borders |
| Muted text | `text-muted-foreground` / `text-slate-500` | ~#64748b | Empty state body text, placeholder labels, inactive tab labels |

Accent reserved for:
- Blue (#2563EB): active tab underline in Config Panel tabs, focus ring on interactive controls, "Ver pensamento" toggle active state
- Violet (#7C3AED): AgentThinking disclosure header background tint (`bg-violet-50`), disclosure chevron icon, rationale section label
- Emerald (#059669): positive reduction badges in Benchmark Scorecard, filled stars when quality >= 4
- Amber (#D97706): mid-range reduction badges, session-change warning notice in Config Panel
- Red (#DC2626): poor reduction badges in Benchmark Scorecard, no destructive actions

---

## Copywriting Contract

All copy in pt-BR. Direct, technical-but-approachable voice. No jargon ("chain-of-thought" → "raciocínio do agente").

### Global / Shared

| Element | Copy |
|---------|------|
| Config Panel tab — Anúncio | "Anúncio" |
| Config Panel tab — Configuração | "Configuração" |
| "Ver pensamento" toggle label (OFF) | "Ver pensamento" |
| "Ver pensamento" toggle label (ON) | "Ocultar pensamento" |
| AgentThinking disclosure trigger (collapsed) | "Raciocínio do agente" |
| AgentThinking disclosure trigger (expanded) | "Raciocínio do agente" |
| Config panel session-change notice | "Alterações valem a partir da próxima negociação." |

### Surface 1 — Config Panel (INTEL-04)

| Element | Copy |
|---------|------|
| Section label — desconto | "Desconto-alvo (%)" |
| Section label — maxRounds | "Máximo de rodadas" |
| Section label — tone | "Tom do agente" |
| Tone option — formal | "Formal" |
| Tone option — casual | "Casual" |
| Section label — anchorStrategy | "Abertura da negociação" |
| Anchor option — agressivo | "Agressiva" |
| Anchor option — moderado | "Moderada" |
| Save button | "Salvar configuração" |
| Save success toast | "Configuração salva." |
| Save error toast | "Erro ao salvar. Tente novamente." |
| Empty / default state (no session) | Config panel renders normally with current localStorage values; no empty state needed |

### Surface 2 — AgentThinking (INTEL-05)

| Element | Copy |
|---------|------|
| Disclosure summary (always visible) | "Raciocínio do agente" |
| Loading state (thinking in progress) | "Analisando..." |
| Empty rationale (no content returned) | "Nenhum raciocínio disponível para esta rodada." |
| Error (failed to parse rationale) | "Não foi possível exibir o raciocínio desta rodada." |
| Toggle tooltip (OFF) | "Ativar exibição do raciocínio" |
| Toggle tooltip (ON) | "Ocultar raciocínio de todos os turnos" |

### Surface 3 — Benchmark Scorecard (/benchmark)

| Element | Copy |
|---------|------|
| Page title | "Benchmark" |
| Page subtitle | "Acompanhe o desempenho das negociações registradas nesta sessão." |
| Empty state heading | "Nenhuma negociação registrada" |
| Empty state body | "Conclua ao menos uma negociação para ver os resultados aqui." |
| Empty state CTA | "Ir para o chat" |
| Column header — data | "Data" |
| Column header — veículo | "Veículo" |
| Column header — redução vs FIPE | "Redução vs FIPE" |
| Column header — rodadas | "Rodadas" |
| Column header — qualidade | "Qualidade" |
| Aggregate card — redução média | "Redução média vs FIPE" |
| Aggregate card — rodadas médias | "Média de rodadas" |
| Aggregate card — qualidade média | "Qualidade média" |
| Aggregate card — total negociações | "Negociações" |
| Rating label — aria | "Avaliação: {N} de 5 estrelas" |
| Rating unset state | "Sem avaliação" |
| No data in aggregate | "—" |
| Error loading from localStorage | "Erro ao carregar histórico. Os dados podem estar corrompidos." |

---

## Surface 1 — Config Panel (INTEL-04)

### Purpose

Replace the left column's `<AdListingForm>` slot with a tabbed panel when needed. Two tabs: "Anúncio" (existing `AdListingForm`) and "Configuração" (new `AgentConfigForm`). Tabs are always accessible. During an active negotiation, saving config shows a warning notice instead of applying immediately.

### Component Tree

```
<LeftColumnPanel>                          ← replaces <aside> in page.tsx left slot
  <Tabs defaultValue="anuncio">            ← shadcn Tabs (radix)
    <TabsList className="w-full">
      <TabsTrigger value="anuncio">Anúncio</TabsTrigger>
      <TabsTrigger value="configuracao">Configuração</TabsTrigger>
    </TabsList>
    <TabsContent value="anuncio">
      <AdListingForm />                    ← existing Phase 1 component, unchanged
    </TabsContent>
    <TabsContent value="configuracao">
      <AgentConfigForm />                  ← new component
    </TabsContent>
  </Tabs>
</LeftColumnPanel>
```

```
<AgentConfigForm>                          ← "use client", reads/writes Zustand agentConfig
  <form onSubmit={handleSave}>
    <div className="space-y-4 p-4">
      <!-- targetDiscount -->
      <div>
        <Label htmlFor="targetDiscount">Desconto-alvo (%)</Label>
        <Input id="targetDiscount" type="number" min={5} max={40} step={1} />
      </div>
      <!-- maxRounds -->
      <div>
        <Label htmlFor="maxRounds">Máximo de rodadas</Label>
        <Input id="maxRounds" type="number" min={2} max={12} step={1} />
      </div>
      <!-- tone -->
      <div>
        <Label>Tom do agente</Label>
        <RadioGroup>
          <RadioGroupItem value="formal" label="Formal" />
          <RadioGroupItem value="casual" label="Casual" />
        </RadioGroup>
      </div>
      <!-- initialAnchorStrategy -->
      <div>
        <Label>Abertura da negociação</Label>
        <RadioGroup>
          <RadioGroupItem value="agressivo" label="Agressiva" />
          <RadioGroupItem value="moderado" label="Moderada" />
        </RadioGroup>
      </div>
      <!-- session-change notice (conditional) -->
      {isNegotiating && (
        <Alert variant="warning">
          <AlertCircle className="h-4 w-4" />
          <AlertDescription>
            Alterações valem a partir da próxima negociação.
          </AlertDescription>
        </Alert>
      )}
      <Button type="submit" className="w-full">Salvar configuração</Button>
    </div>
  </form>
</AgentConfigForm>
```

### Sizing and Spacing

| Property | Value |
|----------|-------|
| Left column width | 320px (unchanged from Phase 1 grid) |
| Tab list height | 36px (`h-9`) |
| Tab content padding | `p-4` (16px) |
| Form field spacing | `space-y-4` (16px between fields) |
| Label-to-input gap | `mt-1.5` (6px, Tailwind default for form fields) |
| Input height | `h-9` (36px, shadcn default) |
| Submit button | full-width `w-full`, height `h-9` |
| Alert padding | shadcn default (`p-4`) |

### Typography

| Element | Classes |
|---------|---------|
| Tab labels | `text-sm font-medium` |
| Field labels | `text-sm font-medium` (shadcn Label default) |
| Input text | `text-sm` |
| Radio option labels | `text-sm` |
| Alert notice text | `text-sm text-amber-700` |
| Submit button text | `text-sm font-medium` |

### Color

| Element | Classes |
|---------|---------|
| TabsList background | `bg-muted` (slate-50 equivalent) |
| Active TabsTrigger | default shadcn active state + blue underline via `data-[state=active]:text-primary` |
| Inactive tab label | `text-muted-foreground` |
| Input border | `border-input` |
| Input focus ring | `ring-primary` (blue) |
| Warning Alert background | `bg-amber-50 border-amber-200 text-amber-700` |
| Warning Alert icon | `text-amber-600` (`AlertCircle` from lucide-react) |
| Submit button | shadcn `Button` default (primary) |

### Interaction States

| State | Behavior |
|-------|----------|
| Idle (no session) | All fields enabled, no warning notice |
| Negotiating (session active) | All fields enabled for editing; amber warning notice rendered below form fields |
| Saving | Button shows `<Loader2 className="mr-2 h-4 w-4 animate-spin" />Salvando...`, disabled |
| Save success | `toast.success("Configuração salva.")` via sonner |
| Save error | `toast.error("Erro ao salvar. Tente novamente.")` via sonner |
| Validation error (out of range) | shadcn FormMessage below the offending input |

### Keyboard Behavior and Accessibility

| Concern | Spec |
|---------|------|
| Tab navigation | Tabs component uses Radix keyboard: Left/Right arrows switch tabs, Enter/Space activates |
| Form fields | Standard sequential Tab order: targetDiscount → maxRounds → tone radio group → anchorStrategy radio group → submit button |
| Radio groups | Arrow keys navigate within each group (Radix RadioGroup default) |
| Submit | Enter on any focused input submits form |
| `aria-label` on Tab triggers | Not needed — text content is sufficient |
| Alert notice | `role="alert"` via shadcn Alert; screen reader announces on mount when negotiating |
| Save button disabled state | `aria-disabled="true"` when saving in progress |

---

## Surface 2 — AgentThinking Disclosure (INTEL-05)

### Purpose

An expandable disclosure rendered inline below each agent MessageBubble, showing the rationale/chain-of-thought for that turn. A global toggle "Ver pensamento" / "Ocultar pensamento" in `NegotiationStatusBar` controls the `defaultOpen` state of all disclosures. Default: all disclosures closed.

### Component Tree

```
<!-- In NegotiationStatusBar — add to right of existing badges -->
<button
  role="switch"
  aria-checked={thinkingVisible}
  aria-label={thinkingVisible ? "Ocultar raciocínio de todos os turnos" : "Ativar exibição do raciocínio"}
  className="ml-auto flex items-center gap-1.5 rounded-md border border-violet-200 bg-violet-50
             px-2 py-1 text-xs font-medium text-violet-700 hover:bg-violet-100
             data-[state=on]:bg-violet-100"
  onClick={toggleThinking}
>
  <Brain className="h-3 w-3" />
  {thinkingVisible ? "Ocultar pensamento" : "Ver pensamento"}
</button>
```

```
<!-- In ChatView, after each agent MessageBubble -->
<AgentThinking
  rationale={message.rationale}          ← string | undefined from store
  isStreaming={message.isStreaming}
  open={thinkingVisible}                 ← controlled by global toggle
/>
```

```
<AgentThinking>                          ← "use client"
  <details
    open={open}
    className="mt-1 rounded-md border border-violet-200 bg-violet-50"
  >
    <summary
      className="flex cursor-pointer items-center gap-1.5 px-3 py-2
                 text-xs font-medium text-violet-700 hover:bg-violet-100
                 list-none select-none"
    >
      <ChevronRight className="h-3 w-3 transition-transform details-open:rotate-90" />
      Raciocínio do agente
    </summary>
    <div className="border-t border-violet-200 px-3 py-2">
      {isStreaming && !rationale ? (
        <!-- Loading state -->
        <p className="text-xs text-muted-foreground italic">Analisando...</p>
      ) : !rationale ? (
        <!-- Empty state -->
        <p className="text-xs text-muted-foreground">
          Nenhum raciocínio disponível para esta rodada.
        </p>
      ) : (
        <!-- Content -->
        <p className="text-xs text-slate-700 leading-relaxed whitespace-pre-wrap">{rationale}</p>
      )}
    </div>
  </details>
</AgentThinking>
```

Implementation note: use native `<details>`/`<summary>` for disclosure. Do not use shadcn Accordion (overkill, adds animation complexity). The `open` prop is passed as a controlled attribute but user can also click summary to toggle individual disclosures.

### Sizing and Spacing

| Property | Value |
|----------|-------|
| Disclosure border radius | `rounded-md` (8px, matches card radius) |
| Summary padding | `px-3 py-2` (12px / 8px) |
| Content padding | `px-3 py-2` (12px / 8px) |
| Chevron icon size | `h-3 w-3` (12px) |
| Brain icon in toggle | `h-3 w-3` (12px) |
| Margin above disclosure | `mt-1` (4px — tight coupling to agent bubble above) |
| Max width | Inherits from MessageBubble container |
| Toggle in StatusBar | `px-2 py-1` |

### Typography

| Element | Classes |
|---------|---------|
| Summary trigger | `text-xs font-medium text-violet-700` |
| Rationale body | `text-xs text-slate-700 leading-relaxed whitespace-pre-wrap` |
| Loading text | `text-xs text-muted-foreground italic` |
| Empty/error text | `text-xs text-muted-foreground` |
| StatusBar toggle | `text-xs font-medium text-violet-700` |

### Color

| Element | Classes |
|---------|---------|
| Disclosure border | `border-violet-200` |
| Disclosure background | `bg-violet-50` |
| Summary hover | `hover:bg-violet-100` |
| Summary text | `text-violet-700` |
| Chevron icon | `text-violet-600` |
| Content area border-top | `border-violet-200` |
| Rationale text | `text-slate-700` |
| Empty/loading text | `text-muted-foreground` |
| StatusBar toggle border | `border-violet-200` |
| StatusBar toggle background | `bg-violet-50` |
| StatusBar toggle text | `text-violet-700` |

### Interaction States

| State | Behavior |
|-------|----------|
| Closed (default) | Summary visible, content hidden via `<details>` collapsed |
| Opened via toggle | All disclosures on page get `open` attribute; each can still be closed individually by clicking summary |
| Streaming / loading | Summary visible; content shows "Analisando..." italic text; no spinner (avoid noise during stream) |
| Complete — with rationale | Full rationale text displayed in content area |
| Complete — no rationale | "Nenhum raciocínio disponível para esta rodada." displayed |
| Error parsing rationale | "Não foi possível exibir o raciocínio desta rodada." displayed |
| Toggle button ON | `aria-checked="true"`, text changes to "Ocultar pensamento" |
| Toggle button OFF | `aria-checked="false"`, text changes to "Ver pensamento" |

### Keyboard Behavior and Accessibility

| Concern | Spec |
|---------|------|
| Toggle button | `role="switch"`, `aria-checked`, focusable with Tab, Space/Enter activates |
| `<summary>` element | Natively keyboard-accessible (Enter/Space toggles, Tab focuses) |
| Screen reader announcement | `<summary>` announces "Raciocínio do agente — expandido/colapsado" |
| Focus order | Toggle in StatusBar → agent bubble → disclosure summary (in DOM order) |
| Icon in summary | `aria-hidden="true"` on Chevron and Brain icons |
| Rationale content | No special role needed — plain paragraph text |

---

## Surface 3 — Benchmark Scorecard (/benchmark)

### Purpose

Dedicated route listing all negotiation sessions stored in localStorage. Allows Felipe/Lucas to annotate each negotiation with a 1–5 star quality rating and view aggregate metrics as a gate criterion for Phase 2 completion.

### Route

`/benchmark` — `src/app/benchmark/page.tsx`

### Component Tree

```
<BenchmarkPage>                             ← src/app/benchmark/page.tsx ("use client")
  <main className="mx-auto max-w-7xl px-6 py-8">
    <header className="mb-6">
      <h1 className="text-2xl font-bold">Benchmark</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Acompanhe o desempenho das negociações registradas nesta sessão.
      </p>
    </header>

    <!-- Aggregate cards row -->
    <div className="mb-6 grid grid-cols-4 gap-4">
      <AggregateCard label="Redução média vs FIPE" value={avgReduction} unit="%" colorClass="text-emerald-700" />
      <AggregateCard label="Média de rodadas" value={avgRounds} unit="" colorClass="text-primary" />
      <AggregateCard label="Qualidade média" value={avgQuality} unit="/5" colorClass="text-violet-700" />
      <AggregateCard label="Negociações" value={total} unit="" colorClass="text-foreground" />
    </div>

    <!-- Negotiation list -->
    {sessions.length === 0 ? (
      <EmptyBenchmark />
    ) : (
      <BenchmarkTable sessions={sessions} onRate={handleRate} />
    )}
  </main>
</BenchmarkPage>
```

```
<AggregateCard>
  <Card>
    <CardHeader className="pb-2">
      <CardTitle className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
        {label}
      </CardTitle>
    </CardHeader>
    <CardContent>
      <div className="text-2xl font-bold {colorClass}">
        {value !== null ? `${value}${unit}` : "—"}
      </div>
    </CardContent>
  </Card>
```

```
<EmptyBenchmark>
  <div className="flex flex-col items-center justify-center rounded-lg border border-dashed
                  border-slate-300 bg-slate-50 py-16 text-center">
    <BarChart2 className="mb-3 h-8 w-8 text-slate-400" aria-hidden="true" />
    <h2 className="text-sm font-medium text-slate-700">Nenhuma negociação registrada</h2>
    <p className="mt-1 text-sm text-muted-foreground">
      Conclua ao menos uma negociação para ver os resultados aqui.
    </p>
    <Button asChild variant="outline" className="mt-4">
      <a href="/">Ir para o chat</a>
    </Button>
  </div>
```

```
<BenchmarkTable sessions={sessions} onRate={handleRate}>
  <div className="overflow-hidden rounded-lg border border-slate-200">
    <table className="w-full text-sm">
      <thead className="bg-slate-50 text-xs font-medium uppercase tracking-wide text-muted-foreground">
        <tr>
          <th className="px-4 py-3 text-left">Data</th>
          <th className="px-4 py-3 text-left">Veículo</th>
          <th className="px-4 py-3 text-right">Redução vs FIPE</th>
          <th className="px-4 py-3 text-right">Rodadas</th>
          <th className="px-4 py-3 text-center">Qualidade</th>
        </tr>
      </thead>
      <tbody className="divide-y divide-slate-200">
        {sessions.map((s) => (
          <BenchmarkRow key={s.id} session={s} onRate={onRate} />
        ))}
      </tbody>
    </table>
  </div>
```

```
<BenchmarkRow session={s} onRate={onRate}>
  <tr className="hover:bg-slate-50">
    <td className="px-4 py-3 text-xs text-muted-foreground">{formatDate(s.endedAt)}</td>
    <td className="px-4 py-3">
      <div className="font-medium">{s.listing.marca} {s.listing.modelo}</div>
      <div className="text-xs text-muted-foreground">{s.listing.ano} • {s.listing.cidade}</div>
    </td>
    <td className="px-4 py-3 text-right">
      <ReductionBadge reduction={s.fipeReductionPct} />
    </td>
    <td className="px-4 py-3 text-right text-sm">
      {s.round}/{s.maxRounds}
    </td>
    <td className="px-4 py-3">
      <StarRating value={s.quality ?? 0} onChange={(v) => onRate(s.id, v)} />
    </td>
  </tr>
```

```
<ReductionBadge reduction={pct}>
  <!-- pct >= 20 → emerald, 10-19 → amber, < 10 → red -->
  <Badge
    variant="outline"
    className={cn(
      pct >= 20
        ? "border-emerald-300 bg-emerald-50 text-emerald-700"
        : pct >= 10
          ? "border-amber-300 bg-amber-50 text-amber-700"
          : "border-red-300 bg-red-50 text-red-700"
    )}
  >
    {pct.toFixed(1)}%
  </Badge>
```

```
<StarRating value={rating} onChange={onChange}>
  <div
    className="flex items-center gap-0.5 min-h-[44px]"
    role="group"
    aria-label="Qualidade da negociação"
  >
    {[1, 2, 3, 4, 5].map((star) => (
      <button
        key={star}
        type="button"
        onClick={() => onChange(star)}
        aria-label={`${star} de 5 estrelas`}
        aria-pressed={value === star}
        className="rounded p-0.5 hover:scale-110 focus-visible:outline-2
                   focus-visible:outline-primary transition-transform"
      >
        <Star
          className={cn(
            "h-4 w-4",
            star <= value
              ? "fill-emerald-500 text-emerald-500"   // filled when <= rating
              : "fill-none text-slate-300"              // empty
          )}
        />
      </button>
    ))}
  </div>
```

### Navigation

Add a link to `/benchmark` in the page header. Since Phase 1 has no explicit `<Header>` component, add a minimal nav bar at the top of `src/app/page.tsx` root layout:

```
<!-- In src/app/layout.tsx or page.tsx — new nav strip -->
<nav className="border-b border-slate-200 bg-white">
  <div className="mx-auto flex max-w-7xl items-center gap-6 px-6 py-3">
    <span className="text-sm font-semibold text-foreground">AutoAgent</span>
    <a href="/" className="text-sm text-muted-foreground hover:text-foreground">Chat</a>
    <a href="/benchmark" className="text-sm text-muted-foreground hover:text-foreground">Benchmark</a>
  </div>
</nav>
```

Active link: add `text-foreground font-medium` to the currently active route link (use `usePathname()` from next/navigation).

### Sizing and Spacing

| Property | Value |
|----------|-------|
| Page max width | `max-w-7xl` (unchanged) |
| Page padding | `px-6 py-8` |
| Header margin-bottom | `mb-6` |
| Aggregate cards grid | `grid-cols-4 gap-4` |
| Aggregate card padding | shadcn Card defaults (`p-6` header, `p-6 pt-0` content) |
| Aggregate card KPI size | `text-2xl font-bold` |
| Table padding per cell | `px-4 py-3` |
| Table header background | `bg-slate-50` |
| Empty state padding | `py-16` |
| Empty state icon size | `h-8 w-8` |
| Star icon size | `h-4 w-4` (16px) |
| Star button touch target | `min-h-[44px]` on wrapper row |
| Nav bar height | `py-3` (~48px effective) |

### Typography

| Element | Classes |
|---------|---------|
| Page title | `text-2xl font-bold` |
| Page subtitle | `text-sm text-muted-foreground` |
| Aggregate card label | `text-xs font-medium uppercase tracking-wide text-muted-foreground` |
| Aggregate card value | `text-2xl font-bold` (color varies by metric) |
| Table header | `text-xs font-medium uppercase tracking-wide text-muted-foreground` |
| Table row — vehicle name | `text-sm font-medium` |
| Table row — vehicle detail | `text-xs text-muted-foreground` |
| Table row — date | `text-xs text-muted-foreground` |
| Table row — rounds | `text-sm` |
| Badge text | `text-xs font-medium` (shadcn Badge default) |
| Empty state heading | `text-sm font-medium text-slate-700` |
| Empty state body | `text-sm text-muted-foreground` |
| Empty state CTA | `text-sm` (shadcn Button outline) |
| Nav links | `text-sm` |

### Color

| Element | Classes |
|---------|---------|
| Page background | `bg-background` (white) |
| Aggregate card — reduction | `text-emerald-700` |
| Aggregate card — rounds | `text-primary` (blue) |
| Aggregate card — quality | `text-violet-700` |
| Aggregate card — total | `text-foreground` |
| Table header background | `bg-slate-50` |
| Row hover | `hover:bg-slate-50` |
| Row dividers | `divide-y divide-slate-200` |
| Reduction >= 20% badge | `border-emerald-300 bg-emerald-50 text-emerald-700` |
| Reduction 10-19% badge | `border-amber-300 bg-amber-50 text-amber-700` |
| Reduction < 10% badge | `border-red-300 bg-red-50 text-red-700` |
| Star filled | `fill-emerald-500 text-emerald-500` |
| Star empty | `fill-none text-slate-300` |
| Empty state border | `border-dashed border-slate-300` |
| Empty state background | `bg-slate-50` |
| Empty state icon | `text-slate-400` |
| Nav bar border | `border-b border-slate-200` |
| Active nav link | `text-foreground font-medium` |
| Inactive nav link | `text-muted-foreground hover:text-foreground` |

### Interaction States

| State | Behavior |
|-------|----------|
| Page load — no sessions | Empty state renders with icon, copy, and CTA button |
| Page load — with sessions | Aggregate cards populate; table rows render |
| Page load — localStorage error | Error message replaces table: "Erro ao carregar histórico. Os dados podem estar corrompidos." with `text-destructive text-sm` |
| Star hover | Individual star button `hover:scale-110` via Tailwind transition |
| Star click | Rating persists to localStorage immediately; no confirmation needed; no toast (interaction is fast + reversible) |
| Rating already set | Stars filled up to current rating; clicking same star clears rating (toggle: `onChange(value === star ? 0 : star)`) |
| Aggregate with no quality ratings | Qualidade média card shows "—" |
| Nav link active | `text-foreground font-medium` applied to matching route |

### Keyboard Behavior and Accessibility

| Concern | Spec |
|---------|------|
| Page `<h1>` | "Benchmark" — single H1 per page |
| Table | `<table>` with `<thead>/<tbody>`, `<th scope="col">` on all headers |
| Star rating group | `role="group"` with `aria-label="Qualidade da negociação"` on wrapper |
| Individual star button | `aria-label="{N} de 5 estrelas"`, `aria-pressed={value === N}` |
| Tab order | Nav → aggregate cards (non-interactive, skip) → table rows → star buttons in each row |
| Star keyboard navigation | Tab moves between star buttons; Space/Enter activates |
| Empty state CTA | Standard `<a>` element, Tab-focusable |
| Aggregate KPIs | Not interactive — no tab stop needed; `aria-hidden` not required |
| Error state | Rendered as `<p role="alert">` for screen reader announcement |
| Nav links | Standard `<a>` elements; active link has visually distinguishable style |
| `BarChart2` icon in empty state | `aria-hidden="true"` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Tabs, TabsList, TabsTrigger, TabsContent, RadioGroup, RadioGroupItem, Alert, AlertDescription, Card, CardHeader, CardContent, CardTitle, Badge, Button, Input, Label | not required |
| No third-party registries | — | not applicable |

New shadcn components required for Phase 2 (not yet in `src/components/ui/`):
- `tabs` — `npx shadcn add tabs`
- `radio-group` — `npx shadcn add radio-group`
- `alert` — `npx shadcn add alert`

Existing components reused without modification: `badge`, `button`, `card`, `input`, `label`, `form`, `skeleton`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
