---
phase: 02-intelig-ncia-do-agente
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - src/components/benchmark/StarRating.tsx
  - src/components/benchmark/__tests__/StarRating.test.tsx
  - src/components/benchmark/ReductionBadge.tsx
  - src/components/benchmark/__tests__/ReductionBadge.test.tsx
  - src/components/benchmark/BenchmarkScorecard.tsx
  - src/components/benchmark/BenchmarkRow.tsx
  - src/components/benchmark/BenchmarkTable.tsx
  - src/components/benchmark/EmptyBenchmark.tsx
  - src/components/Nav.tsx
  - src/app/benchmark/page.tsx
  - src/app/benchmark/__tests__/page.test.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [INTEL-06]

must_haves:
  truths:
    - "Rota /benchmark renderiza 4 cards agregados (Redução, Rodadas, Qualidade, Total) + tabela de sessões OR empty state"
    - "StarRating component toggle pattern (click same star clears rating)"
    - "ReductionBadge muda cor conforme %: ≥20 emerald, 10-19 amber, <10 red"
    - "Nav component mostra AutoAgent + Chat + Benchmark links com usePathname active state"
    - "Layout root renders <Nav /> persistent across routes"
    - "/benchmark Empty state mostra BarChart2 icon + CTA 'Ir para o chat'"
    - "Click em estrela dispatches setUserRating(session.id, rating)"
    - "BenchmarkScorecard usa selectBenchmarkAggregates internally com filter promptVersion='v2.0.0' — gate metrics"
  artifacts:
    - path: "src/app/benchmark/page.tsx"
      provides: "Benchmark route (use client) with aggregates + table + empty state"
    - path: "src/components/Nav.tsx"
      provides: "Persistent nav bar with active state"
    - path: "src/components/benchmark/StarRating.tsx"
      provides: "StarRating input component (1-5 with toggle clear)"
    - path: "src/components/benchmark/BenchmarkScorecard.tsx"
      provides: "4-card aggregate row using selectBenchmarkAggregates"
    - path: "src/components/benchmark/BenchmarkRow.tsx"
      provides: "Table row with date, vehicle, reduction badge, rounds, StarRating"
  key_links:
    - from: "src/app/benchmark/page.tsx"
      to: "selectBenchmarkAggregates + setUserRating"
      via: "store selector + action invoked via useShallow"
      pattern: "selectBenchmarkAggregates|setUserRating"
    - from: "src/components/Nav.tsx"
      to: "usePathname from next/navigation"
      via: "Active link conditional className"
      pattern: "usePathname\\(\\)"
    - from: "src/app/layout.tsx"
      to: "src/components/Nav.tsx"
      via: "<Nav /> rendered inside body persistent"
      pattern: "<Nav />"
---

<objective>
Entregar Surface 3 do UI-SPEC — a rota `/benchmark` completa — e a navegação persistente. Este plano transforma o `history: Session[]` do store (já com ratings + flags desde Plan 03) em scorecard visível para Felipe/Lucas. É o **gate de saída de Phase 2**: sem este plano o INTEL-06 não fecha, porque não há onde exibir agregados nem onde dar rating.

Purpose: Preparar o ambiente para as 20 sessões de benchmark manual que fecham Phase 2. Também introduz nav bar persistente que reusa o pattern em Phase 4 quando entrarem mais rotas (/batch, /export).

Output: 7 novos componentes + 1 nova rota + 1 extensão de layout. ~22 test cases adicionais. Após este plano, fluxo end-to-end completo.
</objective>

<execution_context>
@C:/Users/pc/auto-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/auto-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-intelig-ncia-do-agente/02-CONTEXT.md
@.planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md
@.planning/phases/02-intelig-ncia-do-agente/02-RESEARCH.md
@.planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md
@.planning/phases/02-intelig-ncia-do-agente/02-03-SUMMARY.md
@.planning/phases/02-intelig-ncia-do-agente/02-04-SUMMARY.md

<interfaces>
<!-- Store exports available from Plan 03 -->

From src/lib/stores/negotiation.ts:
- `useNegotiationStore` with slices: history, currentSession, agentConfig, thinkingVisible
- Actions: setUserRating(id, 1|2|3|4|5|undefined), setUserNote(id, string)
- Selectors (module-level): selectBenchmarkAggregates(sessions, filters?), selectGatePass(sessions)

From src/lib/types/session.ts:
```typescript
interface Session {
  // ...Phase 1 fields + Phase 2 additions:
  promptVersion: string;
  appliedConfig: AgentConfig;
  motivationScore?: { score, label, rationale };
  userRating?: 1 | 2 | 3 | 4 | 5;
  userNote?: string;
  finalPrice?: number; // populated by endSession
  flags?: { walkAwayExceeded?, rationaleLeak?, ... };
}
```

From src/app/layout.tsx (Phase 1):
```typescript
export const metadata = { title: "AutoAgent Negotiation Playground" };
export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body className="min-h-screen bg-white text-slate-900 antialiased">
        {children}
      </body>
    </html>
  );
}
```

UI-SPEC locked shapes (02-UI-SPEC.md §Surface 3):
- Page: `<main className="mx-auto max-w-7xl px-6 py-8">`; header h1 "Benchmark" `text-2xl font-bold` + subtitle
- AggregateCards grid: `grid grid-cols-4 gap-4`; card headers `text-xs font-medium uppercase tracking-wide text-muted-foreground`; values `text-2xl font-bold` with colors per metric
- EmptyBenchmark: `py-16 text-center`, BarChart2 icon h-8 w-8, CTA Button asChild variant="outline"
- BenchmarkTable: `<table>` with `<thead>/<tbody>`, thead bg-slate-50, cells px-4 py-3, row hover:bg-slate-50
- ReductionBadge: emerald (>=20) / amber (10-19) / red (<10) outline variant
- StarRating: 5 buttons, Star filled emerald-500 when star <= value / else empty slate-300, min-h-[44px] wrapper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StarRating + ReductionBadge components (leaf atoms) + tests</name>
  <read_first>
    - .planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md §Surface 3 (StarRating linhas 567-595 + ReductionBadge linhas 551-564)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §StarRating.tsx (linhas 1858-1910)
    - src/components/ui/badge.tsx (existing shadcn)
    - src/lib/utils.ts (existing — cn helper)
    - src/components/benchmark/__tests__/StarRating.test.tsx (Wave 0 scaffold)
  </read_first>
  <behavior>
    - **StarRating**: props { value: number (0-5), onChange: (v: number) => void }; 5 buttons in flex row; Star icon from lucide filled emerald-500 when star <= value; toggle pattern on click (value===star ? onChange(0) : onChange(star)); aria-pressed, aria-label per button; role="group" wrapper with aria-label
    - **ReductionBadge**: prop { reduction: number }; uses cn utility + shadcn Badge variant="outline"; colors by threshold
  </behavior>
  <action>
  **Arquivo 1: `src/components/benchmark/StarRating.tsx` (NEW)**

  ```typescript
  "use client";

  import { cn } from "@/lib/utils";
  import { Star } from "lucide-react";

  interface Props {
    value: number; // 0 = unset, 1-5 = rating
    onChange: (v: number) => void;
  }

  export function StarRating({ value, onChange }: Props) {
    return (
      <div
        className="flex items-center gap-1 min-h-[44px]"
        role="group"
        aria-label="Qualidade da negociação"
      >
        {[1, 2, 3, 4, 5].map((star) => (
          <button
            key={star}
            type="button"
            onClick={() => onChange(value === star ? 0 : star)}
            aria-label={`${star} de 5 estrelas`}
            aria-pressed={value === star}
            data-testid={`star-rating-button-${star}`}
            className="rounded p-1 hover:scale-110 focus-visible:outline-2 focus-visible:outline-primary transition-transform"
          >
            <Star
              className={cn(
                "h-4 w-4",
                star <= value ? "fill-emerald-500 text-emerald-500" : "fill-none text-slate-300",
              )}
              aria-hidden="true"
            />
          </button>
        ))}
      </div>
    );
  }
  ```

  **Arquivo 2: `src/components/benchmark/ReductionBadge.tsx` (NEW)**

  ```typescript
  import { Badge } from "@/components/ui/badge";
  import { cn } from "@/lib/utils";

  interface Props {
    reduction: number; // percentage, e.g. 22.5
  }

  export function ReductionBadge({ reduction }: Props) {
    // Info 2 fix: negative reduction means agent overpaid vs FIPE — clamp display to 0.0%
    // while keeping the red color tier (reduction < 10 branch).
    const displayed = Math.max(0, reduction);
    return (
      <Badge
        variant="outline"
        className={cn(
          reduction >= 20
            ? "border-emerald-300 bg-emerald-50 text-emerald-700"
            : reduction >= 10
              ? "border-amber-300 bg-amber-50 text-amber-700"
              : "border-red-300 bg-red-50 text-red-700",
        )}
        data-testid="reduction-badge"
      >
        {displayed.toFixed(1)}%
      </Badge>
    );
  }
  ```

  **Info 2 test** (add to StarRating.test.tsx or create a dedicated ReductionBadge.test.tsx — recommended the latter):

  ```typescript
  // src/components/benchmark/__tests__/ReductionBadge.test.tsx
  import { render, screen } from "@testing-library/react";
  import { describe, expect, it } from "vitest";
  import { ReductionBadge } from "../ReductionBadge";

  describe("ReductionBadge (INTEL-06)", () => {
    it("renders 22.5% with emerald tier when reduction >= 20", () => {
      render(<ReductionBadge reduction={22.5} />);
      const badge = screen.getByTestId("reduction-badge");
      expect(badge).toHaveTextContent("22.5%");
      expect(badge.className).toContain("emerald");
    });

    it("renders 15.0% with amber tier when 10 <= reduction < 20", () => {
      render(<ReductionBadge reduction={15.0} />);
      const badge = screen.getByTestId("reduction-badge");
      expect(badge).toHaveTextContent("15.0%");
      expect(badge.className).toContain("amber");
    });

    it("renders 5.0% with red tier when reduction < 10", () => {
      render(<ReductionBadge reduction={5.0} />);
      const badge = screen.getByTestId("reduction-badge");
      expect(badge).toHaveTextContent("5.0%");
      expect(badge.className).toContain("red");
    });

    it("renders 0.0% red when reduction is negative (agent overpaid)", () => {
      render(<ReductionBadge reduction={-3.2} />);
      const badge = screen.getByTestId("reduction-badge");
      expect(badge).toHaveTextContent("0.0%");
      expect(badge.className).toContain("red");  // tier still determined by raw value
    });
  });
  ```

  **Arquivo 3: `src/components/benchmark/__tests__/StarRating.test.tsx` (FILL)**

  ```typescript
  import { render, screen } from "@testing-library/react";
  import userEvent from "@testing-library/user-event";
  import { describe, expect, it, vi } from "vitest";
  import { StarRating } from "../StarRating";

  describe("StarRating (INTEL-06)", () => {
    it("renders 5 buttons with data-testid star-rating-button-1 through 5", () => {
      render(<StarRating value={0} onChange={() => {}} />);
      for (let i = 1; i <= 5; i++) {
        expect(screen.getByTestId(`star-rating-button-${i}`)).toBeInTheDocument();
      }
    });

    it("value=0 renders all stars empty (fill-none)", () => {
      const { container } = render(<StarRating value={0} onChange={() => {}} />);
      const filled = container.querySelectorAll(".fill-emerald-500");
      expect(filled.length).toBe(0);
    });

    it("value=3 fills 3 stars in emerald", () => {
      const { container } = render(<StarRating value={3} onChange={() => {}} />);
      const filled = container.querySelectorAll(".fill-emerald-500");
      expect(filled.length).toBe(3);
    });

    it("clicking star N calls onChange(N)", async () => {
      const onChange = vi.fn();
      const user = userEvent.setup();
      render(<StarRating value={0} onChange={onChange} />);
      await user.click(screen.getByTestId("star-rating-button-4"));
      expect(onChange).toHaveBeenCalledWith(4);
    });

    it("clicking same star when value=N clears to 0 (toggle)", async () => {
      const onChange = vi.fn();
      const user = userEvent.setup();
      render(<StarRating value={3} onChange={onChange} />);
      await user.click(screen.getByTestId("star-rating-button-3"));
      expect(onChange).toHaveBeenCalledWith(0);
    });

    it("wrapper has role=group and aria-label='Qualidade da negociação'", () => {
      render(<StarRating value={0} onChange={() => {}} />);
      const group = screen.getByRole("group", { name: "Qualidade da negociação" });
      expect(group).toBeInTheDocument();
    });

    it("aria-pressed=true on star matching value", () => {
      render(<StarRating value={4} onChange={() => {}} />);
      expect(screen.getByTestId("star-rating-button-4")).toHaveAttribute("aria-pressed", "true");
      expect(screen.getByTestId("star-rating-button-1")).toHaveAttribute("aria-pressed", "false");
    });
  });
  ```

  Commit: `feat(02): StarRating + ReductionBadge leaf components + StarRating tests (INTEL-06)`.
  </action>
  <verify>
    <automated>pnpm test -- --run src/components/benchmark/__tests__/StarRating.test.tsx</automated>
  </verify>
  <done>
    - 2 new components
    - 7 StarRating test cases GREEN
    - ReductionBadge renders 3 color tiers conditional on reduction threshold
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BenchmarkScorecard + BenchmarkRow + BenchmarkTable + EmptyBenchmark</name>
  <read_first>
    - .planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md §Surface 3 (component tree linhas 449-506 — AggregateCard, EmptyBenchmark, BenchmarkTable, BenchmarkRow)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §BenchmarkScorecard (linhas 1735-1805) + §BenchmarkRow (linhas 1809-1854)
    - src/components/negotiation/SummaryPanel.tsx (analog Phase 1 — 4-card KPI grid)
    - src/lib/stores/negotiation.ts (Wave 3 — selectBenchmarkAggregates export)
    - src/components/benchmark/StarRating.tsx (Task 1)
    - src/components/benchmark/ReductionBadge.tsx
  - src/components/benchmark/__tests__/ReductionBadge.test.tsx (Task 1)
  </read_first>
  <behavior>
    - **BenchmarkScorecard** ("use client"): prop { sessions: Session[] }; useMemo computes aggregates via selectBenchmarkAggregates(sessions, { promptVersion: "v2.0.0" }) — **filter to v2 only for gate metrics**; 4 Cards in grid-cols-4 gap-4 com valores formatados + fallback "—"
    - **BenchmarkRow** ("use client"): props { session: Session, onRate: (id, rating) => void }; renders `<tr>` with 5 cells per UI-SPEC; computes reductionPct from session.finalPrice/fipe; formatDate helper; StarRating bound to session.userRating; touches `session.appliedConfig?.maxRounds ?? session.maxRounds` for backward-compat
    - **BenchmarkTable** ("use client"): props { sessions: Session[], onRate }; renders `<table>` with thead + tbody of BenchmarkRow per session
    - **EmptyBenchmark**: static empty state with BarChart2 icon + title + body + CTA Button asChild linking to "/"
  </behavior>
  <action>
  **Arquivo 1: `src/components/benchmark/BenchmarkScorecard.tsx` (NEW)**

  ```typescript
  "use client";

  import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
  import { selectBenchmarkAggregates } from "@/lib/stores/negotiation";
  import type { Session } from "@/lib/types/session";
  import { useMemo } from "react";

  interface Props {
    sessions: Session[];
  }

  export function BenchmarkScorecard({ sessions }: Props) {
    const aggregates = useMemo(
      () => selectBenchmarkAggregates(sessions, { promptVersion: "v2.0.0" }),
      [sessions],
    );

    return (
      <div className="mb-6 grid grid-cols-4 gap-4">
        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
              Redução média vs FIPE
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-emerald-700" data-testid="benchmark-aggregate-reduction">
              {aggregates.avgReductionPct !== null ? `${aggregates.avgReductionPct.toFixed(1)}%` : "—"}
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
              Média de rodadas
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-primary" data-testid="benchmark-aggregate-rounds">
              {aggregates.avgRounds !== null ? aggregates.avgRounds.toFixed(1) : "—"}
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
              Qualidade média
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-violet-700" data-testid="benchmark-aggregate-quality">
              {aggregates.avgRating !== null ? `${aggregates.avgRating.toFixed(1)}/5` : "—"}
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="pb-2">
            <CardTitle className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
              Negociações
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold text-foreground" data-testid="benchmark-aggregate-total">
              {aggregates.count}
            </div>
          </CardContent>
        </Card>
      </div>
    );
  }
  ```

  **Arquivo 2: `src/components/benchmark/BenchmarkRow.tsx` (NEW)**

  ```typescript
  "use client";

  import type { Session } from "@/lib/types/session";
  import { ReductionBadge } from "./ReductionBadge";
  import { StarRating } from "./StarRating";

  interface Props {
    session: Session;
    onRate: (sessionId: string, rating: 1 | 2 | 3 | 4 | 5 | undefined) => void;
  }

  function formatDate(iso: string | undefined): string {
    if (!iso) return "—";
    try {
      const d = new Date(iso);
      return d.toLocaleDateString("pt-BR", { day: "2-digit", month: "2-digit", year: "numeric" });
    } catch {
      return "—";
    }
  }

  export function BenchmarkRow({ session, onRate }: Props) {
    const reductionPct = session.finalPrice ? (1 - session.finalPrice / session.fipe) * 100 : 0;
    const maxRounds = session.appliedConfig?.maxRounds ?? session.maxRounds;

    return (
      <tr className="hover:bg-slate-50" data-testid={`benchmark-row-${session.id}`}>
        <td className="px-4 py-3 text-xs font-medium text-muted-foreground">{formatDate(session.endedAt)}</td>
        <td className="px-4 py-3">
          <div className="text-sm font-medium">
            {session.listing.marca} {session.listing.modelo}
          </div>
          <div className="text-xs font-medium text-muted-foreground">
            {session.listing.ano} • {session.listing.cidade}
          </div>
        </td>
        <td className="px-4 py-3 text-right">
          {session.finalPrice != null ? <ReductionBadge reduction={reductionPct} /> : <span className="text-sm text-muted-foreground">—</span>}
        </td>
        <td className="px-4 py-3 text-right text-sm font-medium">
          {session.round}/{maxRounds}
        </td>
        <td className="px-4 py-3">
          <StarRating
            value={session.userRating ?? 0}
            onChange={(v) => onRate(session.id, v === 0 ? undefined : (v as 1 | 2 | 3 | 4 | 5))}
          />
        </td>
      </tr>
    );
  }
  ```

  **Arquivo 3: `src/components/benchmark/BenchmarkTable.tsx` (NEW)**

  ```typescript
  "use client";

  import type { Session } from "@/lib/types/session";
  import { BenchmarkRow } from "./BenchmarkRow";

  interface Props {
    sessions: Session[];
    onRate: (sessionId: string, rating: 1 | 2 | 3 | 4 | 5 | undefined) => void;
  }

  export function BenchmarkTable({ sessions, onRate }: Props) {
    return (
      <div className="overflow-hidden rounded-lg border border-slate-200">
        <table className="w-full text-sm">
          <thead className="bg-slate-50 text-xs font-medium uppercase tracking-wide text-muted-foreground">
            <tr>
              <th scope="col" className="px-4 py-3 text-left">Data</th>
              <th scope="col" className="px-4 py-3 text-left">Veículo</th>
              <th scope="col" className="px-4 py-3 text-right">Redução vs FIPE</th>
              <th scope="col" className="px-4 py-3 text-right">Rodadas</th>
              <th scope="col" className="px-4 py-3 text-center">Qualidade</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-slate-200">
            {sessions.map((s) => (
              <BenchmarkRow key={s.id} session={s} onRate={onRate} />
            ))}
          </tbody>
        </table>
      </div>
    );
  }
  ```

  **Arquivo 4: `src/components/benchmark/EmptyBenchmark.tsx` (NEW)**

  ```typescript
  import { Button } from "@/components/ui/button";
  import { BarChart2 } from "lucide-react";

  export function EmptyBenchmark() {
    return (
      <div
        className="flex flex-col items-center justify-center rounded-lg border border-dashed border-slate-300 bg-slate-50 py-16 text-center"
        data-testid="empty-benchmark"
      >
        <BarChart2 className="mb-3 h-8 w-8 text-slate-400" aria-hidden="true" />
        <h2 className="text-sm font-medium text-slate-700">Nenhuma negociação registrada</h2>
        <p className="mt-1 text-sm font-medium text-muted-foreground">
          Conclua ao menos uma negociação para ver os resultados aqui.
        </p>
        <Button asChild variant="outline" className="mt-4">
          <a href="/">Ir para o chat</a>
        </Button>
      </div>
    );
  }
  ```

  Commit: `feat(02): benchmark scorecard + row + table + empty state components (INTEL-06)`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && pnpm test -- --run src/components/benchmark/ 2>&1 | tail -15</automated>
  </verify>
  <done>
    - 4 new components
    - All components use UI-SPEC §Surface 3 exact classes + data-testid
    - pnpm typecheck passes
    - Phase 1 tests unaffected
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Nav component + extend layout.tsx</name>
  <read_first>
    - src/app/layout.tsx (current — 16 lines)
    - .planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md §Surface 3 navigation (linhas 599-613)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §Nav.tsx (linhas 1614-1658) + §layout.tsx (linhas 1917-1943)
  </read_first>
  <behavior>
    - Nav component ("use client"): uses usePathname from next/navigation; shows "AutoAgent" brand span + Chat link (/) + Benchmark link (/benchmark); active link has text-foreground, inactive text-muted-foreground
    - Use plain `<a>` elements (UI-SPEC deliberate decision — não Next/Link, chat-size app, prefetch unnecessary)
    - layout.tsx: import Nav; render `<Nav />` INSIDE body, BEFORE {children}, persists across route changes
  </behavior>
  <action>
  **Arquivo 1: `src/components/Nav.tsx` (NEW)**

  ```typescript
  "use client";

  import { usePathname } from "next/navigation";

  export function Nav() {
    const pathname = usePathname();
    const isActive = (path: string) => pathname === path;

    return (
      <nav className="border-b border-slate-200 bg-white" data-testid="app-nav">
        <div className="mx-auto flex max-w-7xl items-center gap-6 px-6 py-3">
          <span className="text-sm font-medium text-foreground">AutoAgent</span>
          <a
            href="/"
            data-testid="nav-link-chat"
            className={
              isActive("/")
                ? "text-sm font-medium text-foreground"
                : "text-sm font-medium text-muted-foreground hover:text-foreground"
            }
          >
            Chat
          </a>
          <a
            href="/benchmark"
            data-testid="nav-link-benchmark"
            className={
              isActive("/benchmark")
                ? "text-sm font-medium text-foreground"
                : "text-sm font-medium text-muted-foreground hover:text-foreground"
            }
          >
            Benchmark
          </a>
        </div>
      </nav>
    );
  }
  ```

  **Arquivo 2: `src/app/layout.tsx` (EXTEND)**

  ```typescript
  import { Nav } from "@/components/Nav";
  import type { Metadata } from "next";
  import { Toaster } from "sonner"; // se já existia Phase 1
  import "./globals.css";

  export const metadata: Metadata = {
    title: "AutoAgent Negotiation Playground",
    description: "Ferramenta de validação — negociação via agente Claude",
  };

  export default function RootLayout({ children }: { children: React.ReactNode }) {
    return (
      <html lang="pt-BR">
        <body className="min-h-screen bg-white text-slate-900 antialiased">
          <Nav />
          {children}
          <Toaster />
        </body>
      </html>
    );
  }
  ```

  **Nota:** se Phase 1 layout.tsx já tem `<Toaster />` (sonner), preservar. Se não tem, adicionar — outros componentes de Phase 2 (AgentConfigForm) usam `toast.success`.

  **Optional test:** `src/components/__tests__/Nav.test.tsx`:
  ```typescript
  import { render, screen } from "@testing-library/react";
  import { describe, expect, it, vi } from "vitest";
  import { Nav } from "../Nav";

  vi.mock("next/navigation", () => ({
    usePathname: () => "/",
  }));

  describe("Nav", () => {
    it("renders AutoAgent brand + Chat + Benchmark links", () => {
      render(<Nav />);
      expect(screen.getByText("AutoAgent")).toBeInTheDocument();
      expect(screen.getByTestId("nav-link-chat")).toBeInTheDocument();
      expect(screen.getByTestId("nav-link-benchmark")).toBeInTheDocument();
    });

    it("active link on / has text-foreground class", () => {
      render(<Nav />);
      const chatLink = screen.getByTestId("nav-link-chat");
      expect(chatLink.className).toContain("text-foreground");
    });
  });
  ```

  Commit: `feat(02): Nav component + layout.tsx wiring (persistent nav across routes)`.
  </action>
  <verify>
    <automated>pnpm test -- --run src/components/ && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/components/Nav.tsx` exports `Nav`
    - `src/app/layout.tsx` renders `<Nav />` inside body
    - 2 test cases GREEN (optional — if test created)
    - `pnpm build` passes (usePathname from next/navigation compatible with client components)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create /benchmark route page + test integration</name>
  <read_first>
    - src/app/page.tsx (pattern — "use client", useShallow, export default)
    - .planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md §Surface 3 BenchmarkPage (linhas 449-473)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §benchmark/page.tsx (linhas 1664-1729)
    - src/app/benchmark/__tests__/page.test.tsx (Wave 0 scaffold)
    - Outputs de Tasks 1-2: BenchmarkScorecard, BenchmarkTable, EmptyBenchmark, StarRating (para rating integration)
    - src/lib/stores/negotiation.ts (Wave 3 — history + setUserRating)
  </read_first>
  <behavior>
    - "use client"
    - Export default BenchmarkPage
    - useShallow: { history, setUserRating }
    - useMemo endedSessions = history.filter(s => s.status === "ended")
    - Render: header (h1 "Benchmark" + subtitle pt-BR) → BenchmarkScorecard sessions={endedSessions} → conditional: EmptyBenchmark (if length 0) OR BenchmarkTable sessions={endedSessions} onRate={setUserRating}
    - Page wrapper: `<main className="mx-auto max-w-7xl px-6 py-8">`
  </behavior>
  <action>
  **Arquivo 1: `src/app/benchmark/page.tsx` (NEW)**

  ```typescript
  "use client";

  import { BenchmarkScorecard } from "@/components/benchmark/BenchmarkScorecard";
  import { BenchmarkTable } from "@/components/benchmark/BenchmarkTable";
  import { EmptyBenchmark } from "@/components/benchmark/EmptyBenchmark";
  import { useNegotiationStore } from "@/lib/stores/negotiation";
  import { useMemo } from "react";
  import { useShallow } from "zustand/react/shallow";

  export default function BenchmarkPage() {
    const { history, setUserRating } = useNegotiationStore(
      useShallow((s) => ({
        history: s.history,
        setUserRating: s.setUserRating,
      })),
    );

    const endedSessions = useMemo(
      () => history.filter((s) => s.status === "ended"),
      [history],
    );

    return (
      <main className="mx-auto max-w-7xl px-6 py-8">
        <header className="mb-6">
          <h1 className="text-2xl font-bold">Benchmark</h1>
          <p className="mt-1 text-sm font-medium text-muted-foreground">
            Acompanhe o desempenho das negociações registradas nesta sessão.
          </p>
        </header>
        <BenchmarkScorecard sessions={endedSessions} />
        {endedSessions.length === 0 ? (
          <EmptyBenchmark />
        ) : (
          <BenchmarkTable sessions={endedSessions} onRate={setUserRating} />
        )}
      </main>
    );
  }
  ```

  **Arquivo 2: `src/app/benchmark/__tests__/page.test.tsx` (FILL)**

  ```typescript
  import { render, screen } from "@testing-library/react";
  import userEvent from "@testing-library/user-event";
  import { beforeEach, describe, expect, it, vi } from "vitest";
  import BenchmarkPage from "../page";
  import { useNegotiationStore } from "@/lib/stores/negotiation";
  import { DEFAULT_AGENT_CONFIG } from "@/lib/stores/migrations";
  import type { Session } from "@/lib/types/session";

  // Mock next/navigation for Nav-independent rendering (in case page imports trigger it)
  vi.mock("next/navigation", () => ({
    usePathname: () => "/benchmark",
  }));

  function resetStore() {
    useNegotiationStore.setState({
      currentSession: null,
      history: [],
      agentConfig: DEFAULT_AGENT_CONFIG,
      thinkingVisible: false,
    });
  }

  function fakeSession(overrides: Partial<Session> & { id: string }): Session {
    return {
      id: overrides.id,
      listing: { marca: "Honda", modelo: "Civic", ano: 2019, km: 45000, precoPedido: 85000, cidade: "SP", diasOnline: 30, reducoes: 1, motivoDaVenda: "outros" },
      fipe: 82000,
      targetPrice: 61500,
      walkAwayPrice: 73800,
      messages: [],
      round: 4,
      maxRounds: 6,
      status: "ended",
      promptVersion: "v2.0.0",
      appliedConfig: DEFAULT_AGENT_CONFIG,
      finalPrice: 62000,
      endedAt: new Date().toISOString(),
      ...overrides,
    };
  }

  describe("BenchmarkPage (INTEL-06)", () => {
    beforeEach(() => resetStore());

    it("renders 'Nenhuma negociação registrada' when history is empty", () => {
      render(<BenchmarkPage />);
      expect(screen.getByTestId("empty-benchmark")).toBeInTheDocument();
      expect(screen.getByText("Nenhuma negociação registrada")).toBeInTheDocument();
    });

    it("renders 4 aggregate cards + table when ended sessions exist", () => {
      useNegotiationStore.setState({
        history: [fakeSession({ id: "s1" }), fakeSession({ id: "s2" })],
      });
      render(<BenchmarkPage />);
      expect(screen.getByTestId("benchmark-aggregate-reduction")).toBeInTheDocument();
      expect(screen.getByTestId("benchmark-aggregate-rounds")).toBeInTheDocument();
      expect(screen.getByTestId("benchmark-aggregate-quality")).toBeInTheDocument();
      expect(screen.getByTestId("benchmark-aggregate-total")).toBeInTheDocument();
      expect(screen.getByTestId("benchmark-row-s1")).toBeInTheDocument();
      expect(screen.getByTestId("benchmark-row-s2")).toBeInTheDocument();
    });

    it("clicking star in a row dispatches setUserRating with session id", async () => {
      const user = userEvent.setup();
      useNegotiationStore.setState({
        history: [fakeSession({ id: "s1" })],
      });
      render(<BenchmarkPage />);
      // Click the 5th star button in the first row
      const stars = screen.getAllByTestId("star-rating-button-5");
      await user.click(stars[0]);
      expect(useNegotiationStore.getState().history[0].userRating).toBe(5);
    });

    it("empty state CTA button links to '/'", () => {
      render(<BenchmarkPage />);
      const cta = screen.getByText("Ir para o chat");
      expect(cta.closest("a")).toHaveAttribute("href", "/");
    });

    it("aggregate card total reflects endedSessions count (v2 filter)", () => {
      useNegotiationStore.setState({
        history: [
          fakeSession({ id: "s1", promptVersion: "v2.0.0" }),
          fakeSession({ id: "s2", promptVersion: "v2.0.0" }),
          fakeSession({ id: "s3", promptVersion: "v1.0.0" }), // excluded from gate filter
        ],
      });
      render(<BenchmarkPage />);
      const totalCard = screen.getByTestId("benchmark-aggregate-total");
      expect(totalCard).toHaveTextContent("2");
    });

    it("aggregate card reduction computes avg correctly", () => {
      useNegotiationStore.setState({
        history: [
          fakeSession({ id: "s1", fipe: 80000, finalPrice: 60000 }), // 25%
          fakeSession({ id: "s2", fipe: 100000, finalPrice: 75000 }), // 25%
        ],
      });
      render(<BenchmarkPage />);
      const reductionCard = screen.getByTestId("benchmark-aggregate-reduction");
      expect(reductionCard).toHaveTextContent("25.0%");
    });
  });
  ```

  Commit: `feat(02): /benchmark route page + 6 integration tests (INTEL-06 complete)`.
  </action>
  <verify>
    <automated>pnpm test -- --run src/app/benchmark && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/app/benchmark/page.tsx` exists
    - 6 test cases GREEN
    - Page renders within Nav (persistent from layout.tsx)
    - `pnpm build` generates /benchmark route without error
    - End-to-end: Felipe pode navegar / → /benchmark via nav, ver aggregates, avaliar sessões
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userNote field (not in UI this plan but store ready) | React escapes string by default — XSS safe as long as `dangerouslySetInnerHTML` is never used on userNote |
| history[] on page load | Source is localStorage via Zustand persist + migrateV1ToV2 — already a trust fence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure (userNote XSS via rendering) | `src/components/benchmark/BenchmarkRow.tsx` | mitigate | This plan does NOT render userNote (deferred to Phase 4 EXPORT-05 UI). When rendered in future, MUST use `{session.userNote}` (React default-escape), NEVER `dangerouslySetInnerHTML`. Inspect each plan touching userNote with this lint. |
| T-02-05-02 | Denial of Service (localStorage blows up) | `src/app/benchmark/page.tsx` | accept | Phase 2 scope: ~20-50 sessions in history. Zustand persist handles up to ~5MB localStorage comfortably. Phase 3 batch runner may stress — flag for Phase 3 to revisit. |
</threat_model>

<verification>
1. `pnpm test -- --run` — Phase 1 (164) + Waves 0-5 totals ~300+ green
2. `pnpm build` passes — new `/benchmark` route in .next/ output
3. `pnpm lint` passes
4. `grep -n "promptVersion.*v2.0.0" src/components/benchmark/BenchmarkScorecard.tsx` returns match (gate filter applied)
5. `grep -n "dangerouslySetInnerHTML" src/components/benchmark/` returns NOTHING (userNote XSS prevented)
</verification>

<success_criteria>
- 7 new components (StarRating, ReductionBadge, BenchmarkScorecard, BenchmarkRow, BenchmarkTable, EmptyBenchmark, Nav) + 1 new route (/benchmark/page.tsx) + 1 layout extension
- 15+ new test cases GREEN (7 StarRating + 6 BenchmarkPage + 2 Nav optional)
- End-to-end flow works: Felipe starts session on /, ends it, navigates to /benchmark, sees session, clicks stars, rating persists
- UI-SPEC §Surface 3 verbatim reproduction (classes, texts, data-testid)
- `pnpm build` + `pnpm typecheck` + `pnpm lint` all green
</success_criteria>

<output>
Create `.planning/phases/02-intelig-ncia-do-agente/02-05-SUMMARY.md` with:
- 7 new components + their purpose 1-liner
- Nav integration confirmation (layout.tsx includes `<Nav />`)
- /benchmark flow diagram (textual): history[] → filter(ended) → BenchmarkScorecard aggregates (v2-only filter) + BenchmarkTable rows → user clicks star → setUserRating → persisted
- Test count per file
- Confirmation XSS surface (userNote) is NOT yet rendered — deferred to Phase 4
- Final cumulative test count (Phase 1 + Phase 2 total)
</output>
