---
phase: 02-intelig-ncia-do-agente
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/ui/tabs.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/select.tsx
  - src/lib/scoring/__tests__/motivation.test.ts
  - src/lib/data/__tests__/comparables.test.ts
  - src/lib/prompts/__tests__/tone.test.ts
  - src/lib/prompts/__tests__/few-shot.test.ts
  - src/lib/prompts/__tests__/system-v2.test.ts
  - src/lib/schemas/__tests__/agent-config.test.ts
  - src/lib/stores/__tests__/migrations.test.ts
  - src/lib/stores/__tests__/flags.test.ts
  - src/components/negotiation/__tests__/AgentThinking.test.tsx
  - src/components/negotiation/__tests__/AgentConfigForm.test.tsx
  - src/components/negotiation/__tests__/LeftColumnPanel.test.tsx
  - src/components/benchmark/__tests__/StarRating.test.tsx
  - src/app/benchmark/__tests__/page.test.tsx
autonomous: true
requirements: [INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05, INTEL-06]

must_haves:
  truths:
    - "shadcn components tabs, radio-group, alert, select presentes em src/components/ui/"
    - "13 test scaffolds existem, cada um com ao menos um `it.todo` ou `it(...)` que falha ao rodar"
    - "pnpm test -- --run executa sem erro de import (testes falham por lógica, nunca por arquivos faltando)"
  artifacts:
    - path: "src/components/ui/tabs.tsx"
      provides: "shadcn Tabs component (UI-SPEC Surface 1)"
    - path: "src/components/ui/radio-group.tsx"
      provides: "shadcn RadioGroup component (UI-SPEC Surface 1)"
    - path: "src/components/ui/alert.tsx"
      provides: "shadcn Alert component (UI-SPEC Surface 1 D-10 warning)"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select component (D-06 motivoDaVenda)"
    - path: "src/lib/scoring/__tests__/motivation.test.ts"
      provides: "RED test suite for computeMotivationScore (INTEL-01)"
    - path: "src/lib/prompts/__tests__/system-v2.test.ts"
      provides: "RED test suite for buildSystemPromptV2 drift + substitution"
    - path: "src/lib/prompts/__tests__/few-shot.test.ts"
      provides: "RED test suite for alternance + cache_control on last assistant"
    - path: "src/lib/stores/__tests__/migrations.test.ts"
      provides: "RED test suite for migrateV1ToV2"
    - path: "src/lib/stores/__tests__/flags.test.ts"
      provides: "RED test suite for AI-SPEC §6 guardrail flag writers (G-01..G-04) — Wave 3 target"
    - path: "src/components/negotiation/__tests__/AgentThinking.test.tsx"
      provides: "RED test suite for AgentThinking disclosure (INTEL-05)"
  key_links:
    - from: "shadcn add commands"
      to: "src/components/ui/*.tsx"
      via: "pnpm dlx shadcn@latest add <name> executed successfully"
      pattern: "src/components/ui/(tabs|radio-group|alert|select)\\.tsx exists"
    - from: "vitest run"
      to: "test scaffolds"
      via: "pnpm test -- --run exits without 'Cannot find module' errors"
      pattern: "Test Files.*failed, not error-ed"
---

<objective>
Criar Wave 0 — scaffolds de teste (RED state) e instalar componentes shadcn + icons obrigatórios antes de qualquer implementação Phase 2. Sem este plano, TDD é impossível e os waves seguintes quebrariam por imports faltando. Este plano NÃO implementa lógica de negócio — apenas semeia arquivos que outros waves preencherão.

Purpose: Garantir que (a) dependências shadcn/ui/radix necessárias para UI-SPEC Surface 1/2/3 existam, (b) cada behavior determinístico novo (motivation score, tone preset, system-v2 drift guard, few-shot alternance, migrations, flag writers, AgentThinking disclosure) tenha um arquivo de teste vazio pronto para ser preenchido durante Wave 1-5 via TDD loop.

Output: 4 shadcn components + 13 test files scaffolded (todos falham por `it.todo` ou `expect(false).toBe(true)` explícito — nunca por ReferenceError).
</objective>

<execution_context>
@C:/Users/pc/auto-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/auto-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/02-intelig-ncia-do-agente/02-CONTEXT.md
@.planning/phases/02-intelig-ncia-do-agente/02-VALIDATION.md
@.planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md
@.planning/phases/02-intelig-ncia-do-agente/02-RESEARCH.md

<interfaces>
<!-- Pattern convenção já estabelecida Phase 1 para test files -->

Co-location pattern (Phase 1 established):
```typescript
// src/lib/<domain>/<name>.test.ts
import { describe, expect, it } from "vitest";
// Testes importam do sibling file: import { ... } from "./<name>";
```

Wave 0 scaffolds usam `__tests__/` subdirectory ao invés de co-located (per VALIDATION.md Wave 0 list) porque arquivos-alvo ainda não existem — colocar o test em `__tests__/` permite existência antes do source. Cada scaffold deve importar de path que EXISTIRÁ em Wave 1+:

```typescript
// src/lib/scoring/__tests__/motivation.test.ts
import { describe, expect, it } from "vitest";
// import { computeMotivationScore } from "../motivation";  <-- COMENTADO; source nasce em Wave 1

describe("computeMotivationScore (INTEL-01 — Wave 1 target)", () => {
  it.todo("returns BAIXA for zero-signal input");
  it.todo("returns ALTA for max-signal input");
  // ... todos os cases listados
});
```

Para AgentThinking, AgentConfigForm, LeftColumnPanel, StarRating (componentes React):
```typescript
// src/components/negotiation/__tests__/AgentThinking.test.tsx
import { describe, it } from "vitest";
describe("AgentThinking (INTEL-05 — Wave 4 target)", () => {
  it.todo("renders summary 'Raciocínio do agente' when closed");
  // ...
});
```

**Por que `it.todo` e não `expect(false).toBe(true)`:** `it.todo` marca test como pending (reportado mas não falha suite). Vitest aceita `it.todo("...")` sem callback. Acceptance criteria: `pnpm test -- --run` shows pending count matching scaffold count, suite passes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components (tabs, radio-group, alert, select) + verify lucide icons</name>
  <read_first>
    - CLAUDE.md §Constraints (shadcn New York preset locked)
    - .planning/phases/02-intelig-ncia-do-agente/02-UI-SPEC.md §Registry Safety (3 componentes declarados + select adicional)
    - .planning/phases/02-intelig-ncia-do-agente/02-RESEARCH.md §Dependency Surface (4 componentes + 5 icons listados)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §Cross-cutting 2 ("use client")
    - src/components/ui/ (dir listing — confirmar quais shadcn já existem)
    - components.json (shadcn config, já existe)
  </read_first>
  <action>
  Executar os comandos shadcn CLI abaixo no diretório raiz do projeto. Cada comando é idempotente; se um componente já existir, CLI pergunta overwrite — responder "no" (preservar Phase 1).

  ```bash
  pnpm dlx shadcn@latest add tabs
  pnpm dlx shadcn@latest add radio-group
  pnpm dlx shadcn@latest add alert
  pnpm dlx shadcn@latest add select
  ```

  Após instalar, confirmar que cada arquivo foi criado em `src/components/ui/`:
  - `src/components/ui/tabs.tsx`
  - `src/components/ui/radio-group.tsx`
  - `src/components/ui/alert.tsx`
  - `src/components/ui/select.tsx`

  Verificar lucide-react (já instalado Phase 1 per 02-RESEARCH.md §Dependency Surface) — os seguintes icons serão importados em Wave 2-5: `Star`, `Brain`, `BarChart2`, `ChevronRight`, `AlertCircle`. Como lucide expõe named imports sem installation adicional, apenas confirmar que `lucide-react` aparece em `package.json` (já deve estar — Phase 1 KillSwitchBanner usa `AlertCircle`).

  NÃO executar `pnpm install` explicitamente — os adds do shadcn CLI atualizam `package.json` e `pnpm-lock.yaml` automaticamente quando novo radix peer for adicionado (Tabs, RadioGroup, Select exigem `@radix-ui/react-*`).

  **Fallback se `pnpm build` falhar após shadcn adds com erro de missing-module:** executar manualmente
  ```bash
  pnpm install @radix-ui/react-tabs @radix-ui/react-radio-group @radix-ui/react-select
  ```
  (o alert shadcn não requer peer radix específico — usa apenas classes Tailwind + lucide icons). Isso cobre o cenário raro em que o CLI resolve o registry mas não consegue auto-instalar peers por lock conflicts.

  Commit: `feat(02): install shadcn Tabs/RadioGroup/Alert/Select for Phase 2 UI surfaces`.
  </action>
  <verify>
    <automated>test -f src/components/ui/tabs.tsx && test -f src/components/ui/radio-group.tsx && test -f src/components/ui/alert.tsx && test -f src/components/ui/select.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 4 novos arquivos em `src/components/ui/` (tabs, radio-group, alert, select)
    - `pnpm build` passa sem erro (typecheck + transpile OK — confirma peer deps radix resolveram)
    - `package.json` contém `@radix-ui/react-tabs`, `@radix-ui/react-radio-group`, `@radix-ui/react-alert-dialog` (alert base), `@radix-ui/react-select` (OU shadcn usa um single `react-dialog` umbrella — aceitar qualquer resolução válida que `pnpm build` approve)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 13 test scaffolds (RED state — `it.todo` markers per VALIDATION.md)</name>
  <read_first>
    - .planning/phases/02-intelig-ncia-do-agente/02-VALIDATION.md §Wave 0 Requirements (13 paths listados)
    - .planning/phases/02-intelig-ncia-do-agente/02-RESEARCH.md §Validation Architecture (cada test case mapeado com -t flag)
    - .planning/phases/02-intelig-ncia-do-agente/02-PATTERNS.md §Testing idioms (describe/it convention)
    - src/lib/utils/fipe.test.ts (padrão co-located — referência para structure)
    - src/components/negotiation/MessageBubble.test.tsx (padrão component test com @testing-library/react)
  </read_first>
  <action>
  Criar os 13 arquivos abaixo com scaffold mínimo: cada file tem um `describe(...)` principal e uma lista de `it.todo("nome do caso")` para cada test case enumerado em 02-RESEARCH.md §Validation Architecture. NÃO implementar asserts reais — apenas `it.todo`. Import do target é comentado (source nasce em Wave 1+).

  **Arquivo 1:** `src/lib/scoring/__tests__/motivation.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { computeMotivationScore, motivationScoreSchema } from "../motivation";

  describe("computeMotivationScore (INTEL-01 — Wave 1 target)", () => {
    it.todo("returns BAIXA (score < 40) for zero-signal input (0 dias, 0 reduções, outros)");
    it.todo("returns ALTA (score >= 70) for max-signal input (100 dias, 5 reduções, necessidade_financeira)");
    it.todo("applies motivo weights correctly (mudanca_cidade=10, upgrade_veiculo=5, necessidade_financeira=20, outros=0)");
    it.todo("label boundary: score 39 -> BAIXA, 40 -> MÉDIA, 69 -> MÉDIA, 70 -> ALTA");
    it.todo("motivationScoreSchema rejects score < 0 or > 100");
    it.todo("defaults motivoDaVenda to 'outros' when undefined");
    it.todo("rationale string is between 20 and 400 chars");
    it.todo("caps diasOnline contribution at 50 and reducoes at 30");
  });
  ```

  **Arquivo 2:** `src/lib/data/__tests__/comparables.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { COMPARABLES, formatComparablesForPrompt } from "../comparables";

  describe("COMPARABLES (INTEL-02 — Wave 1 target)", () => {
    it.todo("array length between 5 and 8");
    it.todo("every entry has marca, modelo, ano, precoVenda, km, cidade, tempoAteVendaDias");
  });

  describe("formatComparablesForPrompt", () => {
    it.todo("returns a bulletized pt-BR string (one line per comparable starting with '- ')");
    it.todo("formats numbers with pt-BR locale (dot as thousand separator)");
  });
  ```

  **Arquivo 3:** `src/lib/prompts/__tests__/tone.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { TONE_PRESETS, type ToneKey } from "../tone";

  describe("TONE_PRESETS (INTEL-04 D-08 — Wave 1 target)", () => {
    it.todo("has exactly 2 keys: 'formal' and 'casual'");
    it.todo("formal preset starts with 'TOM: Profissional' verbatim per D-08");
    it.todo("casual preset starts with 'TOM: WhatsApp BR' verbatim per D-08");
  });
  ```

  **Arquivo 4:** `src/lib/prompts/__tests__/few-shot.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { buildFewShot } from "../few-shot";

  describe("buildFewShot (INTEL-03 D-12 — Wave 1 target)", () => {
    it.todo("returns MessageParam[] with 8 or 10 or 12 messages (4-6 pairs)");
    it.todo("alternates user/assistant starting with user role");
    it.todo("last element is assistant role (ensures next real user msg is valid)");
    it.todo("last assistant content is TextBlockParam[] carrying cache_control ephemeral");
    it.todo("non-last messages use plain string content (no cache_control)");
    it.todo("does not include literal 'Audi Q5' model strings beyond the canonical pairs themselves");
  });
  ```

  **Arquivo 5:** `src/lib/prompts/__tests__/system-v2.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { SYSTEM_PROMPT_V2_TEMPLATE, PROMPT_VERSION, buildSystemPromptV2 } from "../system-v2";

  describe("SYSTEM_PROMPT_V2_TEMPLATE — drift guard (INTEL-01/02/04/05 — Wave 2 target)", () => {
    it.todo("contains MOTIVAÇÃO DO VENDEDOR block between DADOS DO ANÚNCIO and OBJETIVO");
    it.todo("contains FORMATO INTERNO DE RATIONALE block after FORMATO INTERNO DE ARGUMENTOS");
    it.todo("contains {anchorInstruction} placeholder (D-09 parametrized tática 1)");
    it.todo("contains {tonePreset} placeholder (D-08 replaces hardcoded TOM)");
    it.todo("contains {comparables} placeholder that is NOT a stub (must be substituted)");
    it.todo("exports PROMPT_VERSION === 'v2.0.0'");
  });

  describe("buildSystemPromptV2 — substitution (INTEL-01/02/04 — Wave 2 target)", () => {
    it.todo("substitutes all placeholders (no leftover /\\{[a-zA-Z]+\\}/ tokens in output)");
    it.todo("casual + agressivo + ALTA motivation: output contains 'casual' tone text + '~35%' anchor");
    it.todo("formal + moderado + BAIXA motivation: output contains 'Profissional' tone text + '~20%' anchor");
    it.todo("formal + agressivo + MÉDIA motivation: injects motivation label + '~35%' anchor");
    it.todo("casual + moderado + ALTA motivation: injects motivation rationale + '~20%' anchor");
    it.todo("injects comparables section from formatComparablesForPrompt output (not stub)");
  });
  ```

  **Arquivo 6:** `src/lib/schemas/__tests__/agent-config.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { agentConfigSchema } from "../agent-config";

  describe("agentConfigSchema (INTEL-04 — Wave 1 target)", () => {
    it.todo("accepts all 4 combinations of tone x initialAnchorStrategy");
    it.todo("rejects targetDiscount < 0.05");
    it.todo("rejects targetDiscount > 0.40");
    it.todo("rejects maxRounds < 2 or > 12");
    it.todo("rejects tone outside {formal, casual}");
    it.todo("rejects initialAnchorStrategy outside {agressivo, moderado}");
  });
  ```

  **Arquivo 7:** `src/lib/stores/__tests__/migrations.test.ts`
  ```typescript
  import { describe, it } from "vitest";
  // import { migrateV1ToV2 } from "../migrations";

  describe("migrateV1ToV2 (D-14 — Wave 1 target)", () => {
    it.todo("returns init state when persisted is null");
    it.todo("returns init state when persisted shape is malformed");
    it.todo("preserves all v1 history sessions (same count)");
    it.todo("stamps promptVersion='v1.0.0' on each migrated session");
    it.todo("injects DEFAULT_AGENT_CONFIG into session.appliedConfig");
    it.todo("adds listing.motivoDaVenda='outros' when absent");
    it.todo("leaves motivationScore, userRating, userNote, flags as undefined on v1 sessions");
    it.todo("adds top-level agentConfig with DEFAULT values");
    it.todo("is idempotent: migrateV1ToV2(v2Output) returns equivalent v2Output");
    it.todo("handles v1 with currentSession=null + empty history");
  });
  ```

  **Arquivo 8:** `src/lib/stores/__tests__/flags.test.ts` *(substitui benchmark-selectors.test.ts — selector cases co-localizados em negotiation.test.ts per Phase 1 pattern; ver Plan 03 Task 1)*
  ```typescript
  import { describe, it } from "vitest";
  // import { detectRationaleLeak, detectStrategyLeak, detectMarkdownBleed, detectWalkAwayExceeded } from "../negotiation";

  describe("AI-SPEC §6 guardrail flag writers (INTEL-05/INTEL-06 — Wave 3 target)", () => {
    // G-01 rationaleLeak — regex /<rationale>/i no content pós-strip (runtime defensive — normalmente strip removeria)
    it.todo("rationaleLeak: content contendo tag <rationale> residual após strip dispara flag=true");
    it.todo("rationaleLeak: content limpo sem tags retorna flag=false");

    // G-02 strategyLeakSuspect — regex amplo
    it.todo("strategyLeakSuspect: content mencionando 'targetPrice' dispara flag=true");
    it.todo("strategyLeakSuspect: content mencionando 'walkAway' dispara flag=true");
    it.todo("strategyLeakSuspect: content mencionando 'targetDiscount' dispara flag=true");
    it.todo("strategyLeakSuspect: content mencionando padrão 'R$ X (target)' dispara flag=true");
    it.todo("strategyLeakSuspect: content limpo conversacional retorna flag=false");

    // G-03 markdownBleed — headers, bold, code fence, bullets
    it.todo("markdownBleed: content com '# header' dispara flag=true");
    it.todo("markdownBleed: content com '**bold**' dispara flag=true");
    it.todo("markdownBleed: content com '```' fence dispara flag=true");
    it.todo("markdownBleed: content com '- bullet' em nova linha dispara flag=true");
    it.todo("markdownBleed: content prosa-only (WhatsApp) retorna flag=false");

    // G-04 walkAwayExceeded — parse último R$ do content do agente
    it.todo("walkAwayExceeded: última oferta R$ X < walkAwayPrice (fipe × (1 − targetDiscount × 1.2)) dispara flag=true");
    it.todo("walkAwayExceeded: última oferta R$ X >= walkAwayPrice retorna flag=false");
    it.todo("walkAwayExceeded: content sem R$ numérico retorna flag=false (no-op defensive)");
  });
  ```

  **Arquivo 9:** `src/components/negotiation/__tests__/AgentThinking.test.tsx`
  ```typescript
  import { describe, it } from "vitest";
  // import { AgentThinking } from "../AgentThinking";

  describe("AgentThinking (INTEL-05 — Wave 4 target)", () => {
    it.todo("renders 'Raciocínio do agente' summary always (closed state)");
    it.todo("shows rationale text when open=true and rationale is non-empty");
    it.todo("shows 'Analisando...' italic when isStreaming=true and rationale is undefined");
    it.todo("shows 'Nenhum raciocínio disponível para esta rodada.' when !isStreaming and !rationale");
    it.todo("AgentThinking re-syncs <details> open state when `open` prop changes (remounts via key)");
  });
  ```

  **Arquivo 10:** `src/components/negotiation/__tests__/AgentConfigForm.test.tsx`
  ```typescript
  import { describe, it } from "vitest";
  // import { AgentConfigForm } from "../AgentConfigForm";

  describe("AgentConfigForm (INTEL-04 — Wave 4 target)", () => {
    it.todo("loads default values from agentConfig slice on mount");
    it.todo("submit persists edited values via setAgentConfig action");
    it.todo("FormMessage appears when targetDiscount out of range (5-40)");
    it.todo("alert notice visible when currentSession.status === 'negotiating'");
    it.todo("alert notice hidden when status is idle/ended");
    it.todo("toast.success fires with 'Configuração salva.' on successful submit");
  });
  ```

  **Arquivo 11:** `src/components/negotiation/__tests__/LeftColumnPanel.test.tsx`
  ```typescript
  import { describe, it } from "vitest";
  // import { LeftColumnPanel } from "../LeftColumnPanel";

  describe("LeftColumnPanel (INTEL-04 — Wave 4 target)", () => {
    it.todo("renders AdListingForm inside TabsContent[value=anuncio] by default");
    it.todo("clicking TabsTrigger[value=configuracao] switches to AgentConfigForm");
    it.todo("default tab is 'anuncio'");
  });
  ```

  **Arquivo 12:** `src/components/benchmark/__tests__/StarRating.test.tsx`
  ```typescript
  import { describe, it } from "vitest";
  // import { StarRating } from "../StarRating";

  describe("StarRating (INTEL-06 — Wave 5 target)", () => {
    it.todo("renders 5 buttons with role=button");
    it.todo("value=0 renders all stars empty");
    it.todo("value=3 fills 3 stars in emerald, 2 remain empty");
    it.todo("clicking star N calls onChange(N)");
    it.todo("clicking same star when value=N clears (calls onChange(0))");
    it.todo("wrapper has role=group and aria-label='Qualidade da negociação'");
    it.todo("negative reduction renders as 0.0% red (edge case — Math.max(0, reduction))");
  });
  ```

  **Arquivo 13:** `src/app/benchmark/__tests__/page.test.tsx`
  ```typescript
  import { describe, it } from "vitest";
  // import BenchmarkPage from "../page";

  describe("BenchmarkPage (INTEL-06 — Wave 5 target)", () => {
    it.todo("renders 'Nenhuma negociação registrada' when history is empty");
    it.todo("renders 4 aggregate cards + table when ended sessions exist");
    it.todo("clicking star in a row dispatches setUserRating with session id");
    it.todo("empty state CTA button links to '/'");
  });
  ```

  **Rationale para `it.todo`:** Vitest conta todos como pending; não falham o suite. Isso permite que tests verdadeiros venham nos waves seguintes sem trabalhar em vazio. Quando Wave 1 implementa `computeMotivationScore`, o executor substitui cada `it.todo(...)` por `it(...)` com corpo. Per VALIDATION.md Nyquist, todos os cases acima vêm de §Validation Architecture — copiados verbatim para garantir alinhamento com rubric.

  Commit: `test(02): scaffold 13 Wave 0 test stubs with it.todo placeholders`.
  </action>
  <verify>
    <automated>pnpm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 13 novos arquivos `.test.ts` / `.test.tsx` nos paths listados em VALIDATION.md Wave 0 Requirements
    - `pnpm test -- --run` exit code 0 (tests PASS — `it.todo` counts as pending, not failed)
    - Output shows `XX todo` ou similar count refletindo os test stubs
    - Nenhum `Cannot find module "../motivation"` ou erro de import — imports estão comentados
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI → local filesystem | CLI faz downloads de componentes registrados; risco mitigado por preset pinado (New York) + verified registry (shadcn.com) |
| Test scaffolds → production code | Zero trust-crossing — scaffolds são pure dev artifacts, sem runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | shadcn CLI supply chain | accept | Registry `shadcn` oficial + preset locked em `components.json` (New York). Risco residual equivalente a qualquer `pnpm add` de Phase 1. |
| T-02-00-02 | Denial of Service | `pnpm build` after shadcn add fails to resolve peer deps | mitigate | Task 1 verify step executa `pnpm build` — fail-fast captura qualquer radix peer missing antes de Wave 1. Fallback `pnpm install @radix-ui/react-tabs @radix-ui/react-radio-group @radix-ui/react-select` documented em action. |
</threat_model>

<verification>
## Phase-Level Checks

1. `pnpm build` passa (lint + typecheck + next build) — confirma que shadcn adds não quebraram Phase 1.
2. `pnpm test -- --run` passa com count de pending tests correspondente aos 13 stubs (3-15 `it.todo` cada, ~70-80 total incluindo flags scaffold).
3. `ls src/components/ui/` contém tabs.tsx, radio-group.tsx, alert.tsx, select.tsx (novos).
4. `git diff --stat` mostra apenas arquivos em: `package.json`, `pnpm-lock.yaml`, `src/components/ui/*`, `src/lib/*/__tests__/*`, `src/components/*/__tests__/*`, `src/app/benchmark/__tests__/*`.
</verification>

<success_criteria>
- 4 shadcn components instalados e compile-verified via `pnpm build`.
- 13 test scaffolds criados, cada um com ≥2 `it.todo` cases extraídos de 02-VALIDATION.md / 02-RESEARCH.md §Validation Architecture.
- `pnpm test -- --run` exit 0 com pending count > 0 (RED-friendly, não ERROR).
- Zero modificações em arquivos Phase 1 (sem regressão).
- Git history: um commit de shadcn + um commit de scaffolds (atômicos).
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelig-ncia-do-agente/02-00-SUMMARY.md` with:
- Lista dos 4 shadcn components instalados (com versões radix extraídas de package.json)
- Lista dos 13 test files criados, contagem total de `it.todo` markers
- Confirmação `pnpm build` + `pnpm test -- --run` green
- Baseline de métricas: total tests antes vs. depois (sobe pending count, passed count estável em 164)
</output>
