---
phase: 08-scraping-pipeline
plan: 09
plan_id: 08-09
slug: phase-gate-setup
title: Phase gate — Apify dashboard config + setup runbook + scripts/setup-apify-schedule.ts + smoke test
type: execute
wave: 4
depends_on: [08-06, 08-07, 08-08]
files_modified:
  - scripts/setup-apify-schedule.ts
  - .planning/phases/08-scraping-pipeline/08-09-SETUP.md
autonomous: false
requirements_addressed: [SCRAPE-01, SCRAPE-02, SCRAPE-05, SCRAPE-06]
tags: [scraping, runbook, smoke-test, phase-gate]
must_haves:
  truths:
    - "scripts/setup-apify-schedule.ts can idempotently create or update an Apify schedule pointing at autoagente.ai/api/scrape/webmotors/webhook with TARGET_MODELS as startUrls and the X-AutoAgent-Webhook-Secret header"
    - "08-09-SETUP.md documents every Vercel env var the developer must set: SCRAPE_WEBHOOK_SECRET, MAX_DAILY_SCRAPE_COST_USD (optional, default 50), CRON_SECRET, APIFY_API_TOKEN"
    - "A real end-to-end smoke test is executed: one real Apify scheduled run, against staging or prod webhook, produces at least 1 listings row + 1 scrape_runs row + 0 unresolved errors"
    - "The full Phase 8 test suite (`pnpm test`) is green at the moment of the smoke test"
  artifacts:
    - path: "scripts/setup-apify-schedule.ts"
      provides: "Idempotent script that creates/updates the Apify schedule via REST API; accepts dev | prod env arg"
      contains: "TARGET_MODELS"
    - path: ".planning/phases/08-scraping-pipeline/08-09-SETUP.md"
      provides: "Step-by-step deployment runbook: env vars + Apify dashboard config + cron verification + first-run smoke checklist"
      contains: "## Smoke test checklist"
  key_links:
    - from: "scripts/setup-apify-schedule.ts"
      to: "https://api.apify.com/v2/schedules"
      via: "fetch POST/PUT with token query param"
      pattern: "apify\\.com/v2/schedules"
    - from: "scripts/setup-apify-schedule.ts"
      to: "src/lib/apify/target-models.ts"
      via: "import { TARGET_MODELS }"
      pattern: "import \\{ TARGET_MODELS \\}"
---

<objective>
Phase gate. Three deliverables:

1. **`scripts/setup-apify-schedule.ts`** — A re-runnable script that creates/updates the Apify scheduled run via Apify REST `/v2/schedules`. Reads `TARGET_MODELS` from `src/lib/apify/target-models.ts` to build the actor input, and configures the webhook target as `https://autoagente.ai/api/scrape/webmotors/webhook` (or staging) with the `X-AutoAgent-Webhook-Secret` header sourced from `process.env.SCRAPE_WEBHOOK_SECRET`. Without this script, the schedule + webhook config lives only in the Apify dashboard (NOT in git) — a single dashboard reset would silently break production. With the script, redeploying the schedule is one command.

2. **`.planning/phases/08-scraping-pipeline/08-09-SETUP.md`** — A deployment runbook the developer follows once at deploy time: which env vars to set in Vercel, how to run `setup-apify-schedule.ts`, how to verify the cron jobs are registered, and the smoke-test checklist for the first real scheduled run.

3. **HUMAN smoke test gate** — Developer executes one real scheduled run end-to-end (Apify schedule fires → webhook receives → listings inserted → scrape_runs row recorded). This is a checkpoint task; phase is NOT complete until the developer signs off in the SUMMARY.

Output: 1 new script + 1 new runbook + a HUMAN gate.
</objective>

<execution_context>
@C:/Users/pc/auto-agent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/auto-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scraping-pipeline/08-CONTEXT.md
@.planning/phases/08-scraping-pipeline/08-RESEARCH.md
@.planning/phases/08-scraping-pipeline/08-PATTERNS.md
@src/lib/apify/target-models.ts
@src/app/api/scrape/webmotors/webhook/route.ts
@src/app/api/cron/listings-cleanup/route.ts
@src/app/api/cron/fipe-retry/route.ts
@vercel.json

<interfaces>
<!-- Apify Schedules REST API (verified docs.apify.com/platform/schedules + /v2/schedules): -->

<!-- LIST schedules: GET /v2/schedules?token={token} -->
<!--   Returns { data: { items: [...] } } -->

<!-- CREATE schedule: POST /v2/schedules?token={token} -->
<!--   Body: -->
<!--   { -->
<!--     name: "autoagent-webmotors-prod", -->
<!--     cronExpression: "0 2,14 * * *", -->
<!--     timezone: "America/Sao_Paulo", -->
<!--     isEnabled: true, -->
<!--     actions: [ -->
<!--       { -->
<!--         type: "RUN_ACTOR", -->
<!--         actorId: "ribtools~webmotors-scraper", -->
<!--         runInput: { startUrls: [{url}, ...], maxItems: 2000, proxyConfiguration: { useApifyProxy: true } } -->
<!--       } -->
<!--     ] -->
<!--   } -->

<!-- UPDATE schedule: PUT /v2/schedules/{scheduleId}?token={token} -->
<!--   Same body shape -->

<!-- Webhook target separately configured on the actor or via Apify integration tab: -->
<!--   POST /v2/actor-tasks/{taskId}/webhooks?token={token} -->
<!--   But for scheduled runs, webhooks are configured at the run-input level via -->
<!--   the `webhooks` field of the run input or the Apify dashboard's Webhooks UI. -->
<!--   Since dashboard UI is the simplest path, this script PRINTS the webhook -->
<!--   config the developer must paste into the dashboard rather than automating it. -->

<!-- Cron expressions (locked in CONTEXT D-03): -->
<!--   prod: "0 2,14 * * *" America/Sao_Paulo -->
<!--   dev:  "0 3 * * *"     America/Sao_Paulo -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/setup-apify-schedule.ts</name>
  <read_first>
    - C:\Users\pc\auto-agent\.planning\phases\08-scraping-pipeline\08-RESEARCH.md (§ Locked Decisions Table — D-03 cron expressions; § Runtime State Inventory — Apify dashboard config is the only out-of-git state)
    - C:\Users\pc\auto-agent\.planning\phases\08-scraping-pipeline\08-CONTEXT.md (§ Anti-patterns — DO NOT use Apify SDK; use direct fetch)
    - C:\Users\pc\auto-agent\src\lib\apify\target-models.ts (created in plan 08-02 — source of startUrls)
    - C:\Users\pc\auto-agent\scripts\probe-apify-tier.ts (created in plan 08-01 — same conventions: token-scrub, fetch-only, exit codes)
    - C:\Users\pc\auto-agent\src\app\api\scrape\webmotors\route.ts (lines 86-101 — `SCRAPER_ACTOR = "ribtools~webmotors-scraper"` constant for reuse)
  </read_first>
  <files>scripts/setup-apify-schedule.ts</files>
  <action>
Create `scripts/setup-apify-schedule.ts` with the following exact behavior:

1. CLI usage: `pnpm tsx scripts/setup-apify-schedule.ts <env>` where `<env>` is `dev` or `prod`. Exit 2 if missing or invalid.

2. Read env vars (exit 2 with stderr message if any required is missing):
   - `APIFY_API_TOKEN` — required.
   - `SCRAPE_WEBHOOK_SECRET` — required (used to print the dashboard config developer must paste).
   - `WEBHOOK_BASE_URL` — optional; defaults to:
     - `dev`: `http://localhost:3000`
     - `prod`: `https://autoagente.ai`

3. Constants (hardcode):
   ```typescript
   const APIFY_BASE = "https://api.apify.com/v2";
   const ACTOR = "ribtools~webmotors-scraper";
   const TIMEZONE = "America/Sao_Paulo";
   const SCHEDULE_NAMES = {
     dev: "autoagent-webmotors-dev",
     prod: "autoagent-webmotors-prod",
   };
   const CRON = {
     dev: "0 3 * * *",       // 03:00 BRT
     prod: "0 2,14 * * *",   // 02:00 + 14:00 BRT
   };
   const MAX_ITEMS_PER_RUN = 2000;
   ```

4. Import `TARGET_MODELS` from `@/lib/apify/target-models` — actually, since this is a script not under `src/`, use a relative import: `import { TARGET_MODELS } from "../src/lib/apify/target-models";`. (If TS resolution fails, use `tsx`'s tsconfig path support — `pnpm tsx` handles this.)

5. Build `startUrls`:
   ```typescript
   const startUrls = TARGET_MODELS.map((m) => ({ url: m.url }));
   ```

6. List existing schedules via `GET ${APIFY_BASE}/schedules?token=${encodeURIComponent(token)}`. Find one whose `name === scheduleName`. If found, capture its `id` for PUT; otherwise POST a new one.

7. Build the request body:
   ```typescript
   const body = {
     name: scheduleName,
     cronExpression: cron,
     timezone: TIMEZONE,
     isEnabled: true,
     actions: [
       {
         type: "RUN_ACTOR",
         actorId: ACTOR,
         runInput: {
           startUrls,
           maxItems: MAX_ITEMS_PER_RUN,
           proxyConfiguration: { useApifyProxy: true, apifyProxyGroups: ["RESIDENTIAL"] },
         },
       },
     ],
   };
   ```

8. Issue:
   - If existing: `PUT ${APIFY_BASE}/schedules/${existingId}?token=...` with body
   - If new: `POST ${APIFY_BASE}/schedules?token=...` with body

   Either way, expect 200/201. On non-OK status, print scrubbed error and exit 1.

9. After success, PRINT the webhook config the developer must paste into the Apify dashboard manually (Apify webhooks are configured at the actor or schedule level via UI; this script does NOT automate that step because the dashboard's headers-template UI is the simplest path):

   ```
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Schedule synced. Now configure the webhook in the Apify dashboard:

   1. Open https://console.apify.com/schedules/<schedule-id>/webhooks
      (the URL is also in the JSON above)
   2. Click "Add webhook"
   3. Event types: "Run finished" (ACTOR.RUN.SUCCEEDED)
   4. Request URL: <WEBHOOK_BASE_URL>/api/scrape/webmotors/webhook
   5. Headers (JSON template):
      {
        "Content-Type": "application/json",
        "X-AutoAgent-Webhook-Secret": "<paste your SCRAPE_WEBHOOK_SECRET>"
      }
   6. Save

   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ```

   The actual `SCRAPE_WEBHOOK_SECRET` is NEVER printed by the script — only the placeholder text. Developer pastes the env var value into the dashboard manually.

10. Token-scrub policy: every console output and thrown error MUST pass through `String(x).replace(new RegExp(token, "g"), "[REDACTED]")`. Same discipline as plan 08-01.

11. NO npm dependencies added. Pure built-in fetch + ESM.

Top-of-file shebang/comment: explain this is the Phase 8 schedule-sync script, idempotent, safe to re-run.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit scripts/setup-apify-schedule.ts</automated>
  </verify>
  <acceptance_criteria>
- File `scripts/setup-apify-schedule.ts` exists.
- `pnpm tsc --noEmit scripts/setup-apify-schedule.ts` exits 0.
- `grep -F 'TARGET_MODELS' scripts/setup-apify-schedule.ts` returns at least one match (used to build startUrls).
- `grep -F 'apifyProxyGroups: ["RESIDENTIAL"]' scripts/setup-apify-schedule.ts` returns a match (per plan 08-01 probe outcome).
- `grep -F '"0 2,14 * * *"' scripts/setup-apify-schedule.ts` returns a match (prod cron).
- `grep -F '"0 3 * * *"' scripts/setup-apify-schedule.ts` returns a match (dev cron).
- `grep -F 'America/Sao_Paulo' scripts/setup-apify-schedule.ts` returns a match.
- `grep -F 'replace(new RegExp(token' scripts/setup-apify-schedule.ts` returns at least one match (token-scrub).
- `grep -F 'X-AutoAgent-Webhook-Secret' scripts/setup-apify-schedule.ts` returns a match (mentioned in printed instructions).
- `grep -F 'process.exit(2)' scripts/setup-apify-schedule.ts` returns at least one match (missing env branch).
- The literal `SCRAPE_WEBHOOK_SECRET` (the env var name) MUST appear ONLY in instruction text and as a `process.env.` reference — verify the actual value is never echoed by checking the script's output flow doesn't `console.log(secret)` directly. Specifically: `grep -F 'console.log(' scripts/setup-apify-schedule.ts` outputs lines must not include any string interpolation of `SCRAPE_WEBHOOK_SECRET` env value (review manually if grep result is non-empty).
  </acceptance_criteria>
  <done>
Schedule-sync script exists, typechecks, references TARGET_MODELS, never echoes the webhook secret value, scrubs token from all output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 08-09-SETUP.md runbook</name>
  <read_first>
    - C:\Users\pc\auto-agent\.planning\phases\08-scraping-pipeline\08-CONTEXT.md (full file)
    - C:\Users\pc\auto-agent\.planning\phases\08-scraping-pipeline\08-RESEARCH.md (§ Environment Availability — every env var enumerated; § Runtime State Inventory)
    - C:\Users\pc\auto-agent\vercel.json (post-08-08 — both cron entries present)
  </read_first>
  <files>.planning/phases/08-scraping-pipeline/08-09-SETUP.md</files>
  <action>
Create `.planning/phases/08-scraping-pipeline/08-09-SETUP.md` with the following EXACT structure (fill in each section verbatim — these are deployment instructions, not boilerplate):

```markdown
---
phase: 08-scraping-pipeline
plan: 09
type: runbook
created: 2026-04-25
---

# Phase 8 — Deployment Runbook

> Read this end-to-end before pushing the Phase 8 PR to production. The Apify
> schedule + webhook config is the ONE part of Phase 8 that lives outside git;
> losing the dashboard config breaks production silently.

## Prerequisites

- [ ] Plan 08-01 probe returned `Decision: GO` (`.planning/phases/08-scraping-pipeline/08-01-PROBE-RESULT.md`).
- [ ] All Phase 8 tests pass: `pnpm test --run` exits 0.
- [ ] Whole-repo typecheck + lint green: `pnpm tsc --noEmit && pnpm lint`.
- [ ] Phase 7 cross-phase contract tests still pass: `pnpm test src/lib/matching/ --run`.

## Step 1 — Vercel env vars

Add these to Vercel dashboard (Settings → Environment Variables):

| Name | Value | Why | Required |
|------|-------|-----|----------|
| `APIFY_API_TOKEN` | (from Apify console → Account → Integrations) | Apify REST calls + schedule sync | YES |
| `SCRAPE_WEBHOOK_SECRET` | Generate with `openssl rand -base64 32` | Webhook auth (X-AutoAgent-Webhook-Secret header) | YES |
| `MAX_DAILY_SCRAPE_COST_USD` | `50` (default — omit unless overriding) | D-02 cost cap | NO |
| `CRON_SECRET` | Generate with `openssl rand -base64 32`; reuse if already set from Phase 6/7 | Vercel Cron auth on listings-cleanup + fipe-retry | YES |

Add to BOTH "Production" and "Preview" environments. The "Development" environment is fine without them — local dev tests use mocked clients.

## Step 2 — Apify schedule sync

Run from your local machine after the env vars above are set in `.env.local`:

```bash
# Dev (1 run/day at 03:00 BRT)
pnpm dotenv -e .env.local -- pnpm tsx scripts/setup-apify-schedule.ts dev

# Prod (2 runs/day at 02:00 + 14:00 BRT)
pnpm dotenv -e .env.local -- pnpm tsx scripts/setup-apify-schedule.ts prod
```

Expected output: a JSON dump of the created/updated schedule + a "configure the webhook" instruction block.

Re-running is safe — the script is idempotent (PUT-by-name).

## Step 3 — Apify webhook config (manual, dashboard)

Per the script's printed instructions:

1. Open https://console.apify.com/schedules — find `autoagent-webmotors-prod` (or `-dev`).
2. Click into the schedule → "Webhooks" tab → "Add webhook".
3. Configure:
   - Event types: `ACTOR.RUN.SUCCEEDED` (only — do NOT enable FAILED/ABORTED, we don't have a path for those yet)
   - Request URL: `https://autoagente.ai/api/scrape/webmotors/webhook` (prod) or your dev URL
   - Headers (JSON template):
     ```json
     {
       "Content-Type": "application/json",
       "X-AutoAgent-Webhook-Secret": "<paste the SCRAPE_WEBHOOK_SECRET value here>"
     }
     ```
   - Payload template: leave default (Apify sends `{ resource: { id }, eventType, ... }`).
4. Save.

## Step 4 — Vercel Cron verification

After deploy:

1. Vercel Dashboard → Project → Cron Jobs tab.
2. Verify two entries are listed:
   - `/api/cron/listings-cleanup` — Daily, 04:00 UTC
   - `/api/cron/fipe-retry` — Hourly, minute 5
3. Optionally: trigger each manually via the Vercel UI's "Run now" button. Expected:
   - listings-cleanup: 200 + `{ "removed": 0 }` (zero rows are stale yet on a fresh deploy)
   - fipe-retry: 200 + `{ "processed": 0, "succeeded": 0, "failed": 0 }`

## Smoke test checklist

This is the phase gate. Do NOT mark Phase 8 complete until ALL of these are checked.

- [ ] Manually trigger one Apify scheduled run (Apify Dashboard → schedule → "Run now").
- [ ] Watch Vercel logs (`vercel logs --follow` or Dashboard → Functions → /api/scrape/webmotors/webhook).
- [ ] Within 5 minutes, observe a single `200` log entry for the webhook with non-zero `listings_new` in the response body.
- [ ] In Supabase Dashboard → Table Editor → `scrape_runs`: confirm one new row with `status='completed'`, populated `cost_usd`, `listings_new > 0`.
- [ ] In Supabase Dashboard → Table Editor → `listings`: confirm new rows appear with `source='webmotors'` and recent `last_scraped_at`.
- [ ] Token-scrub spot check: search Vercel logs for the literal value of `APIFY_API_TOKEN` and `SCRAPE_WEBHOOK_SECRET`. Both MUST return zero hits. (If either appears, file an immediate issue and rotate the secret.)
- [ ] Cost cap test (optional, careful): temporarily set `MAX_DAILY_SCRAPE_COST_USD=0.01` in Vercel preview env, manually post a fake Apify-shape webhook with a valid secret, expect 429.

## Rollback procedure

If a scheduled run causes problems:

1. Apify Dashboard → schedule → toggle "Enabled" off. (Stops new runs immediately.)
2. If listings table is corrupted: `UPDATE listings SET status='removed' WHERE last_scraped_at > '<timestamp of bad run>'`. (Soft delete — Phase 9 matching skips status='removed' per existing engine.ts contract.)
3. The webhook route stays enabled but receives no traffic. The cron jobs continue running benignly.
4. Investigate via `scrape_runs` history (filter `status='failed'` or sort by `listings_error DESC`).

## Known operational concerns

- **Apify dashboard config drift:** The webhook URL + headers config is NOT in git. If someone edits or deletes it in the dashboard, the Apify run still SUCCEEDS but our webhook never fires → silent ingest gap. Mitigation: re-run `setup-apify-schedule.ts` after any dashboard change; the script does not (yet) restore webhook config — that step remains manual until Phase 13.
- **Akamai escalation:** If WebMotors deploys a stricter anti-bot upgrade, the ribtools actor may break. Watch for `scrape_runs.listings_new == 0` for 3 consecutive runs; that's the warning signal. Mitigation: contact actor maintainer or rotate to a different actor.
- **Parallelum FIPE downtime:** D-04 retry queue absorbs short outages (<24h). Sustained outages > 24h grow the queue unbounded; consider a Phase 13 max-age purge.

## Sign-off

After all smoke-test boxes are checked, append to `08-09-SUMMARY.md`:

```
## Phase 8 sign-off

- Smoke test executed: <ISO timestamp>
- First scheduled run id: <Apify run id>
- listings_new in first run: <N>
- listings_error in first run: <N>
- scrape_runs id: <UUID>
- Confirmed by: <developer name>
```
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/08-scraping-pipeline/08-09-SETUP.md','utf8'); for(const m of ['## Step 1','## Step 2','## Step 3','## Step 4','## Smoke test checklist','## Rollback procedure','## Sign-off']){ if(!c.includes(m)){console.error('missing:',m); process.exit(1)} }"</automated>
  </verify>
  <acceptance_criteria>
- The verify command exits 0 (all required sections present).
- `grep -F 'SCRAPE_WEBHOOK_SECRET' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns at least 3 matches (env var, header config, smoke test scrub check).
- `grep -F 'CRON_SECRET' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns at least 1 match.
- `grep -F 'APIFY_API_TOKEN' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns at least 2 matches.
- `grep -F 'MAX_DAILY_SCRAPE_COST_USD' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns at least 1 match.
- `grep -F '/api/cron/listings-cleanup' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns a match.
- `grep -F '/api/cron/fipe-retry' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns a match.
- `grep -F 'X-AutoAgent-Webhook-Secret' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns a match.
- `grep -F 'autoagente.ai/api/scrape/webmotors/webhook' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns a match.
- `grep -F 'Decision: GO' .planning/phases/08-scraping-pipeline/08-09-SETUP.md` returns a match (probe-result prerequisite).
  </acceptance_criteria>
  <done>
Runbook written; every env var documented; manual Apify dashboard steps explicit; smoke test checklist actionable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — execute the smoke test and sign off</name>
  <what-built>
The full Phase 8 pipeline:
- `lib/apify/` modules (types, target-models, webhook-auth, filters, webmotors-normalize, client) — all unit-tested
- `/api/scrape/webmotors/webhook` extended with body-shape discriminator, cost cap, scrape_runs lifecycle, dataset streaming, FIPE retry queue
- `/api/cron/listings-cleanup` (D-05 stale TTL 72h) and `/api/cron/fipe-retry` (D-04) cron routes
- `vercel.json` with both cron schedules
- `scripts/probe-apify-tier.ts` (Wave 0 blocker probe)
- `scripts/setup-apify-schedule.ts` (idempotent Apify schedule sync)
- `.planning/phases/08-scraping-pipeline/08-09-SETUP.md` runbook

What's still NOT in git:
- Apify dashboard webhook config (URL + headers) — must be set manually per runbook Step 3
- Vercel env vars — must be set manually per runbook Step 1
  </what-built>
  <how-to-verify>
1. Verify all prior plans are committed:
   ```bash
   git log --oneline | head -20
   ```
   Confirm commits exist for each of: 08-01 probe, 08-02 types+target-models, 08-03 webhook-auth, 08-04 filters+normalize, 08-05 client, 08-06 webhook extension, 08-07 listings-cleanup cron, 08-08 fipe-retry cron, 08-09 setup script + runbook.

2. Run the entire test suite:
   ```bash
   pnpm test --run
   pnpm tsc --noEmit
   pnpm lint
   ```
   ALL must exit 0. If anything red, do NOT proceed.

3. Open `.planning/phases/08-scraping-pipeline/08-09-SETUP.md` and execute Steps 1-4 (Vercel env vars, schedule sync, Apify webhook dashboard config, cron verification).

4. Execute the "Smoke test checklist" in the runbook end-to-end. Every box must be checked.

5. After smoke test passes, append the "Phase 8 sign-off" block to `08-09-SUMMARY.md` with the actual values from your run (timestamp, run id, counts, scrape_runs id, your name).

6. Commit the sign-off:
   ```bash
   git add .planning/phases/08-scraping-pipeline/08-09-SUMMARY.md
   git commit -m "docs(phase-08): smoke test signed off — pipeline live"
   ```

Required outcomes:
- Apify scheduled run completes with status SUCCEEDED
- Webhook receives the Apify-shape payload, returns 200
- `scrape_runs` row written with status='completed', cost_usd populated, listings_new > 0
- New `listings` rows in DB with source='webmotors' and recent last_scraped_at
- Zero hits when grepping Vercel logs for raw token/secret values
- All Phase 8 unit tests still pass after the smoke test (no environmental side effects)

If ANY of these fail:
- Smoke test fails → reply "BLOCKED — <reason>"
- Phase 8 stays open until the issue is resolved
  </how-to-verify>
  <resume-signal>Reply "SIGNED OFF" when the runbook smoke-test checklist is fully checked and the sign-off block is committed in 08-09-SUMMARY.md. Reply "BLOCKED — <reason>" if any smoke-test step failed. Anything else pauses the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local script → Apify REST API (schedule create/update) | Trusted developer machine; APIFY_API_TOKEN handled |
| Local script → stdout (developer's terminal) | Should never echo SCRAPE_WEBHOOK_SECRET (developer pastes manually into dashboard) |
| Vercel env vars → runtime | Trusted; deployment is the only writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Information Disclosure: setup-apify-schedule.ts logs APIFY_API_TOKEN | scripts/setup-apify-schedule.ts | mitigate | Token-scrub regex applied to all stdout/stderr/throw output (acceptance criteria asserts the literal `replace(new RegExp(token` is present). Same discipline as plan 08-01. |
| T-08-09-02 | Information Disclosure: script prints SCRAPE_WEBHOOK_SECRET in dashboard config block | scripts/setup-apify-schedule.ts | mitigate | The printed instructions use a placeholder (`<paste your SCRAPE_WEBHOOK_SECRET>`). The actual value is NEVER read from env or echoed by the script — developer copies it from `.env.local` to the dashboard manually. Acceptance criteria includes a manual review of `console.log` lines for direct env-value interpolation. |
| T-08-09-03 | Tampering: schedule-sync overwrites unrelated schedules | scripts/setup-apify-schedule.ts | mitigate | List-by-name match before PUT/POST. Names are explicit constants `autoagent-webmotors-{dev|prod}`. No wildcard or pattern match. Idempotent (re-running is a no-op). |
| T-08-09-04 | Spoofing: someone runs the script in their account and points the schedule at a malicious webhook | scripts/setup-apify-schedule.ts | accept | Script requires the developer's own APIFY_API_TOKEN. If an attacker has the token they have full account access regardless. The webhook URL is hardcoded by env (`WEBHOOK_BASE_URL` defaults to autoagente.ai prod) — attacker would need to override the env to redirect, which means they already have shell access to the developer machine. Defense-in-depth via the manual webhook configuration step (Apify webhook URL is set in dashboard, NOT by this script). |
| T-08-09-05 | Repudiation: smoke-test sign-off is not auditable | 08-09-SUMMARY.md sign-off block | mitigate | Sign-off block requires explicit timestamp + run id + counts + developer name; committed to git for permanent record. |
| T-08-09-06 | Tampering: developer skips smoke-test and signs off blindly | HUMAN gate | accept | Trust the developer. The runbook checklist is comprehensive; the gate's resume-signal demands "SIGNED OFF" with the implied checklist completion. Phase 8 review (verify-work) will spot-check the live pipeline state. |
</threat_model>

<verification>
- `pnpm tsc --noEmit scripts/setup-apify-schedule.ts` exits 0
- All grep-based acceptance criteria for the script and runbook pass
- The runbook section-presence check exits 0
- HUMAN smoke test is signed off in 08-09-SUMMARY.md
</verification>

<success_criteria>
- `scripts/setup-apify-schedule.ts` exists, typechecks, is idempotent, scrubs token, never echoes the webhook secret value
- `.planning/phases/08-scraping-pipeline/08-09-SETUP.md` exists with all 7 required sections, lists every env var, gives explicit dashboard steps, and includes a comprehensive smoke-test checklist
- HUMAN smoke test executed end-to-end against the real Apify schedule + webhook + cron + DB
- Phase 8 sign-off block committed to 08-09-SUMMARY.md with actual run id, counts, timestamp
- Apify schedule + webhook + cron jobs running in production
- listings table populated with new WebMotors rows from the first scheduled run
- Zero token leaks (verified by Vercel logs spot check)
</success_criteria>

<output>
After completion, create `.planning/phases/08-scraping-pipeline/08-09-SUMMARY.md` (the sign-off block in Task 3 is appended to this same file).
</output>
