---
id: 260422-001
slug: root-to-appshell
description: Root / renders AppShell (v3 dashboard) instead of PlaygroundModule
date: 2026-04-22
status: complete
---

# Quick Task 260422-001 — Summary

## What changed

`src/app/page.tsx` now renders `AppShell` (the full v3 dashboard with signup gate, sidebar, and 9 modules) instead of the isolated `PlaygroundModule`.

## Why

`workspace.autoagente.ai` (DNS + Vercel domain added 2026-04-22) points at the root of the Next.js project. Before this change, hitting the root URL served the ugly Phase 1 chat in isolation. Now it serves the intended v3 dashboard directly — no redirect, clean URL for Friday's demo.

## Files touched

- `src/app/page.tsx` — swapped import (`PlaygroundModule` → `AppShell`) and component returned. Renamed the exported function `NegotiationPage` → `RootPage` to reflect new role.

## Not touched (intentional)

- `src/app/app/page.tsx` — `/app` route preserved as fallback
- `src/components/v3/modules/PlaygroundModule.tsx` — still accessible as the `playground` module inside AppShell's module switcher

## Verification

- Type check: `pnpm tsc --noEmit` — passed (silent, no errors)
- Dev server: ran on `:3001`, both `/` and `/app` returned HTTP 200
- Rendered content at `/` contains "Marketplace" + "onboarding" strings (AppShell/SignupView markers); no `PlaygroundModule` content in HTML
- `pnpm build` — pre-existing WasmHash failure in local environment, unrelated to this change (reproduces on main pre-edit). Vercel build environment is unaffected

## Demo impact

- `workspace.autoagente.ai/` → v3 dashboard (SignupView gate on first visit, then module switcher)
- `autoagente.ai` and `www.autoagente.ai` → landing Astro intact (zero DNS changes on apex/www)
