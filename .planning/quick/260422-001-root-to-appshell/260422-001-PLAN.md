---
id: 260422-001
slug: root-to-appshell
description: Root / renders AppShell (v3 dashboard) instead of PlaygroundModule
date: 2026-04-22
status: in-progress
---

# Quick Task 260422-001: Root to AppShell

## Context

`workspace.autoagente.ai` (new Vercel subdomain, DNS propagating) points at the root of the Next.js project. Today `src/app/page.tsx` renders `PlaygroundModule` — the isolated Phase 1 chat. The v3 dashboard (Sidebar, Marketplace, Backstage, signup gate, 9 modules) lives at `/app` via `src/app/app/page.tsx`.

For Friday's demo we want the root URL to show the full v3 dashboard directly — no redirect, clean URL.

## Tasks

### T1 — Swap root page component

**Files:** `src/app/page.tsx`

**Action:** Replace `PlaygroundModule` import/render with `AppShell` import/render. No other changes.

**Verify:**
- `src/app/page.tsx` imports `AppShell` from `@/components/v3/AppShell`
- Component returns `<AppShell />`
- `src/app/app/page.tsx` remains untouched (preserves `/app` route as fallback)
- `pnpm tsc --noEmit` passes (type check)
- `pnpm build` succeeds

**Done when:** root route `/` renders the v3 dashboard (with signup gate on first visit).

## must_haves

- Root `/` serves the v3 AppShell — confirmed by reading `src/app/page.tsx`
- `/app` route still compiles and works — confirmed by `src/app/app/page.tsx` untouched
- Type check + build pass
