/**
 * /privacidade — LGPD privacy policy stub. Real content TBD in Phase 13b.
 */

export const metadata = {
  title: "Privacidade — AutoAgente",
};

export default function PrivacidadePage() {
  return (
    <main className="mx-auto min-h-screen max-w-2xl px-6 py-16 text-slate-900 dark:text-slate-100">
      <h1
        className="text-3xl font-semibold tracking-tight"
        style={{ fontFamily: "var(--font-fraunces, Georgia, serif)" }}
      >
        Política de Privacidade
      </h1>
      <p className="mt-4 text-slate-600 dark:text-slate-300">
        Em construção. A versão final — incluindo detalhes sobre tratamento de dados sob LGPD,
        retenção, e opt-out — fica disponível antes do go-live com tráfego real.
      </p>
    </main>
  );
}
